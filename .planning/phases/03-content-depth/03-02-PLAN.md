---
phase: 03-content-depth
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/[lang]/get-tenant-faq.ts
  - src/app/[lang]/get-tenant-faq.test.ts
  - src/components/AnswerBlock.tsx
  - src/components/Accordion.tsx
  - src/config/types.ts
requirements: [CONTENT-01, CONTENT-02]
autonomous: true

must_haves:
  truths:
    - "getTenantFaq(locale) returns a NEW array merging per-tenant FAQ items first then the de-tenanted base items (intent order per D-06), never mutating either source"
    - "FaqItem type carries q, a, and optional link {href,label}; the a field is always clean plain text so faqPageGraph acceptedAnswer.text stays link-free (D-30)"
    - "<AnswerBlock> is a server component (no use client) rendering exactly one <h1> heading plus a visible crawlable <p> with at most one inline link, no CTA chrome (D-16/D-18)"
    - "Accordion renders an optional inline link after the answer text when item.link is present, keeping the schema-fed plain a text untouched (D-30)"
    - "Loader uses @/ aliases and server-only; it is NEVER imported by schema-invariants.ts (Pitfall 1/4)"
  artifacts:
    - path: "src/app/[lang]/get-tenant-faq.ts"
      provides: "getTenantFaq loader (react cache, server-only), mergeFaqItems pure fn, FaqItem re-export"
      contains: "getTenantFaq"
    - path: "src/app/[lang]/get-tenant-faq.test.ts"
      provides: "Tests for merge order (tenant-first), immutability, and plain-text a field"
      contains: "mergeFaqItems"
    - path: "src/components/AnswerBlock.tsx"
      provides: "Server component: h1 heading + factual <p> + optional single inline link"
      contains: "function AnswerBlock"
    - path: "src/components/Accordion.tsx"
      provides: "FAQ accordion extended with optional per-item inline link"
      contains: "item.link"
    - path: "src/config/types.ts"
      provides: "FaqItem type with optional link field"
      contains: "FaqItem"
  key_links:
    - from: "src/app/[lang]/get-tenant-faq.ts"
      to: "src/config/tenants/ongles-maily/faq.fr.json"
      via: "static @/ import of per-tenant faq stub, merged with base dict faq.items"
      pattern: "faq\\.fr\\.json"
    - from: "src/components/Accordion.tsx"
      to: "src/config/types.ts"
      via: "renders FaqItem.link inline anchor after the plain a paragraph"
      pattern: "item\\.link"
---

<objective>
Build the shared runtime foundation both content slices consume: the `FaqItem` type (with optional `link`), the server-only `getTenantFaq()` loader (per-tenant + base merge, intent-ordered per D-06), the `<AnswerBlock>` server component (carries the page `<h1>`, D-18/D-19), and the `<Accordion>` link extension (D-30). No content is authored here — the loader composes the empty stubs created in 03-01 and the components render whatever copy lands in 03-03/03-04. Build stays green.

Purpose: Isolate the wiring/transform layer (loader merge logic, component contract) from content authoring so the two parallel content slices in Wave 3 plug into a stable, tested interface.
Output: `get-tenant-faq.ts` (+ test), `AnswerBlock.tsx`, extended `Accordion.tsx`, `FaqItem` type in `types.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-depth/03-CONTEXT.md
@.planning/phases/03-content-depth/03-RESEARCH.md

@src/app/[lang]/dictionaries.ts
@src/app/[lang]/seo-content.ts
@src/components/Accordion.tsx
@src/components/PageHeader.tsx
@src/config/types.ts

# RESEARCH §D-02/D-06 gives the exact getTenantFaq merge pattern (static @/
# imports of all tenant faq JSON, react cache, [...tenantItems, ...base]).
# RESEARCH §h1 audit gives the <AnswerBlock> component contract.
# get-tenant-faq.ts is a NORMAL runtime module — it CAN use @/ aliases. It must
# NEVER be imported by schema-invariants.ts (Pitfall 1/4): the build guard uses
# its OWN relative-path JSON imports (added in 03-01), separate code path.
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: FaqItem type + getTenantFaq loader with merge tests</name>
  <files>src/config/types.ts, src/app/[lang]/get-tenant-faq.ts, src/app/[lang]/get-tenant-faq.test.ts</files>
  <behavior>
    - mergeFaqItems(base, tenantItems) returns tenantItems followed by base (tenant-first intent order, D-06)
    - mergeFaqItems returns a NEW array; the input base and tenantItems arrays are not mutated (immutability)
    - Every returned item has a string a field (plain text, no markup) — link is a separate optional field
    - With empty stubs (base 11 items + tenant 0 items in 03-01 state) the merged length equals the base length
    - FaqItem type accepts {q,a} (base shape) and {q,a,link:{href,label}} (tenant shape) without type error
  </behavior>
  <action>
    Add a `FaqItem` type to `src/config/types.ts`: `{ q: string; a: string; link?: { href: string; label: string } }` (per D-30 research: `a` is always clean plain text; `link` optional/nullable). Keep it compatible with the existing base `{q,a}` shape so `dictionaries/{en,fr}.json` items satisfy it.

    Create `src/app/[lang]/get-tenant-faq.ts` (server-only runtime module — CAN use `@/` aliases, mirrors `dictionaries.ts`/`seo-content.ts`). Follow RESEARCH §D-02/D-06 exactly: `import "server-only"`, `import { cache } from "react"`, static `@/` imports of `frDict`/`enDict` faq base and all four tenants' `faq.{locale}.json` (ongles-maily, ongles-charlesbourg, ongles-rivieres, template). Build a `baseFaq: Record<Locale, readonly FaqItem[]>` from `frDict.faq.items`/`enDict.faq.items` and a `tenantFaq: Record<string, Record<Locale, readonly FaqItem[]>>` from the per-tenant `.items`. Implement a PURE `mergeFaqItems(base, tenantItems): FaqItem[]` returning `[...tenantItems, ...base]` (new array, never mutate — tenant high-intent facts lead, D-06). Implement `resolveTenantFaq(locale)` reading `tenant.id` from `@/config`, falling back to `[]` per side. Export `getTenantFaq = cache(resolveTenantFaq)` and re-export the `FaqItem` type.

    Write `src/app/[lang]/get-tenant-faq.test.ts` (bun:test) FIRST covering every <behavior> case against `mergeFaqItems` directly (it is a pure exportable fn — test it without the server-only loader). Confirm RED, implement to GREEN. Do NOT call `getTenantFaq` from any build-time/guard code path.
  </action>
  <verify>
    <automated>bun test src/app/\[lang\]/get-tenant-faq.test.ts</automated>
  </verify>
  <done>FaqItem type exported from types.ts; getTenantFaq + mergeFaqItems implemented; merge is tenant-first and immutable; all merge tests GREEN; bun run build still succeeds; loader not referenced by schema-invariants.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AnswerBlock server component (h1 + factual prose, ≤1 link)</name>
  <files>src/components/AnswerBlock.tsx</files>
  <behavior>
    - Renders exactly one <h1> element carrying the heading prop
    - Renders the text prop inside a single visible <p> (real prose, no aria-hidden, no display:none)
    - When link prop is present, appends exactly one inline <a> after the text; when absent, no anchor renders
    - No "use client" directive; no button/CTA chrome inside the block (D-16)
  </behavior>
  <action>
    Create `src/components/AnswerBlock.tsx` per RESEARCH §h1 audit contract. Server component (NO `"use client"`). Props: `{ heading: string; text: string; link?: { href: string; label: string } }`. Render a `<section>` wrapper, a single `<h1>` with the `heading`, and a single `<p>` containing `text` plus — only when `link` is present — one trailing inline `<a href={link.href}>{link.label}</a>` (D-16: at most one inline link, no CTA button chrome). Prose must be visible and in the accessibility tree (D-18): no `aria-hidden`, no hidden classes. Match existing Tailwind tokens used in the codebase (espresso/mocha/sand) consistent with PageHeader for visual continuity. This component carries the page `<h1>` (D-19) — it is the single h1 on every page it appears on.
  </action>
  <verify>
    <automated>grep -L "use client" src/components/AnswerBlock.tsx && grep -c "<h1" src/components/AnswerBlock.tsx</automated>
  </verify>
  <done>AnswerBlock.tsx is a server component rendering one h1 + one visible p + optional single inline link; no use client; no CTA chrome; bun run build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Accordion optional inline link (D-30 schema-clean)</name>
  <files>src/components/Accordion.tsx, src/config/types.ts</files>
  <behavior>
    - Accordion accepts readonly FaqItem[] (q, a, optional link)
    - When an item has link, an inline <a href label> renders after the plain answer <p>; when absent, no anchor
    - The plain a text rendered is unchanged (no link markup injected into the a string) — keeps schema text clean (D-30)
  </behavior>
  <action>
    Extend `src/components/Accordion.tsx` to type `items` as `readonly FaqItem[]` (import `FaqItem` from `@/config/types`) instead of the inline `{q,a}` literal. After the existing answer `<p>{item.a}</p>`, conditionally render one inline `<a href={item.link.href}>{item.link.label}</a>` when `item.link` is present (D-16 ≤1 link). Do NOT inject the link into the `a` string — `a` stays clean plain text so `faqPageGraph(items)` keeps `acceptedAnswer.text` link-free (D-30). Preserve the existing `<details>/<summary>` crawlable structure and keyboard accessibility. Surgical change only — do not restyle existing markup.
  </action>
  <verify>
    <automated>bun test src/ 2>&1 | tail -5 && grep -c "item.link" src/components/Accordion.tsx</automated>
  </verify>
  <done>Accordion typed with FaqItem; renders optional inline link after the plain answer; a text untouched; existing FAQ tests still GREEN; bun run build succeeds.</done>
</task>

</tasks>

<verification>
- `bun test src/app/[lang]/get-tenant-faq.test.ts` GREEN (merge tenant-first + immutable + plain-text a)
- `AnswerBlock.tsx` is a server component (no `use client`), exactly one `<h1>`, no CTA chrome
- `Accordion.tsx` renders optional inline link without polluting the `a` plain text (D-30)
- `bun test src/` full suite GREEN (existing tests unaffected; D-05/D-11 still RED from 03-01)
- `bun run build` succeeds (guards still unwired; loader is request-time only)
- `getTenantFaq` not referenced anywhere under `next.config.ts`/`schema-invariants.ts` import graph
</verification>

<success_criteria>
FaqItem type with optional link; getTenantFaq loader merges tenant-first immutably; AnswerBlock server component carries single h1; Accordion supports optional inline link with clean schema text; build green; loader isolated from build guard.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author→git | New components/loader consume author-written JSON; no user input path |
| request→render | getTenantFaq runs at request time under force-dynamic; server-only prevents client bundle leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | get-tenant-faq.ts leaking into build guard graph | mitigate | Loader is server-only request-time only; schema-invariants.ts uses separate relative-path JSON imports (verified by build) |
| T-03-04 | Information disclosure | XSS via answer/link text | accept | All copy is author-written JSON committed to git; React escapes string output by default (RESEARCH Security Domain) |
| T-03-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH Standard Stack: zero new deps) — no install task |
</threat_model>

<output>
Create `.planning/phases/03-content-depth/03-02-SUMMARY.md` when done
</output>
