---
phase: 03-content-depth
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/dictionaries/fr.json
  - src/dictionaries/en.json
  - src/config/tenants/ongles-maily/faq.fr.json
  - src/config/tenants/ongles-maily/faq.en.json
  - src/config/tenants/ongles-charlesbourg/faq.fr.json
  - src/config/tenants/ongles-charlesbourg/faq.en.json
  - src/config/tenants/ongles-rivieres/faq.fr.json
  - src/config/tenants/ongles-rivieres/faq.en.json
  - src/app/[lang]/faq/page.tsx
requirements: [CONTENT-02]
autonomous: true

must_haves:
  truths:
    - "The shared base faq.items in dictionaries/{en,fr}.json is de-tenanted: the 4 tenant-specific items (location, hours, booking, services-offered) are removed and the remaining generic items drop the Ongles Maily brand name (D-03)"
    - "charlesbourg and rivieres no longer render Ongles Maily facts in their FAQ — each tenant location/hours/booking fact comes from its own faq.{locale}.json (D-03 defect fix)"
    - "Each live tenant has >= 20 distinct Q&A pairs per locale (base + tenant union) with FR/EN structural parity (D-04)"
    - "The /faq page renders the intent-ordered base+tenant union via getTenantFaq and feeds the SAME union to faqPageGraph so mainEntity count == rendered count (F-01)"
    - "Per-tenant faq.{locale}.json items are authored in intent order (location, hours, booking, pricing, services, trust) so the tenant-first merge yields D-06 ordering"
  artifacts:
    - path: "src/config/tenants/ongles-maily/faq.fr.json"
      provides: ">=13 per-tenant FAQ items (4 migrated + 9 new) in intent order, real maily facts"
      contains: "items"
    - path: "src/dictionaries/fr.json"
      provides: "De-tenanted base faq.items (~9 generic items, no brand name)"
      contains: "items"
    - path: "src/app/[lang]/faq/page.tsx"
      provides: "FAQ render + JSON-LD both fed by getTenantFaq union"
      contains: "getTenantFaq"
  key_links:
    - from: "src/app/[lang]/faq/page.tsx"
      to: "src/app/[lang]/get-tenant-faq.ts"
      via: "getTenantFaq(lang) feeds BOTH faqPageGraph(union) and Accordion items={union}"
      pattern: "getTenantFaq"
    - from: "src/config/tenants/ongles-maily/faq.fr.json"
      to: "src/config/tenants/ongles-maily/faq.en.json"
      via: "FR/EN item-count + key-structure parity (guarded by seo-parity.test.ts from 03-01)"
      pattern: "items"
---

<objective>
Vertical content slice A — FAQ depth (CONTENT-02). After this slice a real user visiting `/faq` on any tenant sees that tenant OWN location/hours/booking facts plus generic salon answers, at least 20 per locale, in intent order — and the cross-tenant FAQ defect (charlesbourg/rivieres showing Ongles Maily facts) is gone. De-tenant the shared base (D-03), author per-tenant `faq.{locale}.json` (D-02/D-04), and switch `/faq` render plus JSON-LD to the merged union (F-01).

This slice is gated by the D-05 floor guard tests authored RED in 03-01: once content lands, those tests flip GREEN (the guard wiring into the build happens in 03-05). FR is authored first as source-of-truth (D-21), EN is native-quality idiomatic (D-24), both committed together to keep parity (Pitfall 2).

Purpose: Make Success Criterion 2 TRUE (>=20 Q&A per tenant per locale, FR keys identical to EN) and fix the per-tenant FAQ correctness defect.
Output: De-tenanted base dict faq; per-tenant faq.{locale}.json for 3 live tenants (fr+en); `/faq` consuming getTenantFaq union.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-depth/03-CONTEXT.md
@.planning/phases/03-content-depth/03-RESEARCH.md

@src/dictionaries/fr.json
@src/dictionaries/en.json
@src/app/[lang]/faq/page.tsx
@src/app/[lang]/get-tenant-faq.ts
@src/config/tenants/ongles-maily/faq.fr.json
@src/lib/store-config.ts

# RESEARCH §D-04/D-03 gives the exact item migration table (items 1,2,4,5 to
# per-tenant; items 3,6,7,8,9,10,11 stay in base, de-branded) and the 9 new-item
# cluster plan. Per-tenant FACTS (address, hours, phone) come from
# getStoreConfig()/tenant config — read each tenant config; NEVER hardcode another
# tenant facts (multi-tenant rule). RESEARCH §D-30 gives the {q,a,link?} shape;
# the a field stays clean plain text.
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-tenant the shared base FAQ (D-03)</name>
  <files>src/dictionaries/fr.json, src/dictionaries/en.json</files>
  <action>
    Per D-03 + RESEARCH §D-04 migration table: in BOTH src/dictionaries/fr.json and en.json faq.items, REMOVE the 4 tenant-specific items (location, hours, booking with the hardcoded phone, services-offered). KEEP the generic items (sans rendez-vous policy, hygiene/tools, gift certificates, languages FR/EN, fill frequency, satisfaction guarantee, payment methods) but REWRITE any that name "Ongles Maily" to be brand-agnostic (use le salon / the salon, or nous / we; FR register vous per D-21). Per RESEARCH add 2 new truly-generic Health & Trust items to the base (dust collector during manicure; product-sensitivity policy) so the base lands at ~9 generic items. Answer style: answer-first, 1-3 short sentences ~40-60 words (D-07/D-22). Update fr and en in the SAME commit with identical key structure (Pitfall 2). Do NOT touch content.{locale}.json (D-26). After edit, the base must contain ZERO tenant-specific facts and ZERO brand names.
  </action>
  <verify>
    <automated>bun test src/config/seo/seo-parity.test.ts -t "F-02" &amp;&amp; node -e "const f=require('./src/dictionaries/fr.json'),e=require('./src/dictionaries/en.json'); const t=JSON.stringify(f.faq)+JSON.stringify(e.faq); if(/Ongles Maily|Carrefour Beauport/.test(t)){console.error('brand leak');process.exit(1)} if(f.faq.items.length!==e.faq.items.length){console.error('count mismatch');process.exit(1)} console.log('base items',f.faq.items.length)"</automated>
  </verify>
  <done>Base faq.items has no tenant facts and no brand names; fr/en counts equal (~9 items each); F-02 dict parity GREEN; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Author per-tenant faq.{locale}.json for 3 live tenants (D-02/D-04/D-06)</name>
  <files>src/config/tenants/ongles-maily/faq.fr.json, src/config/tenants/ongles-maily/faq.en.json, src/config/tenants/ongles-charlesbourg/faq.fr.json, src/config/tenants/ongles-charlesbourg/faq.en.json, src/config/tenants/ongles-rivieres/faq.fr.json, src/config/tenants/ongles-rivieres/faq.en.json</files>
  <action>
    Per D-01 (hybrid scope: shared de-tenanted base + per-tenant layer, both feeding the union): for EACH live tenant (ongles-maily, ongles-charlesbourg, ongles-rivieres) read that tenant own config/getStoreConfig facts (address, hours, phone, booking URL) — NEVER copy another tenant facts. Replace the empty `{ "items": [] }` stub (from 03-01) with >=13 items per locale, authored in INTENT ORDER (D-06): location, hours, booking, then pricing, services, trust/logistics. Factual overlap with the answer blocks is allowed but vary the wording — no verbatim on-page duplication, each surface independently citable (D-27). Use the {q,a,link?} shape (D-30): a is clean plain text; add an optional link only where natural (booking/locations), at most one per item (D-16). Content set per RESEARCH §D-04: the 4 migrated items (location with real postal code, hours, booking with real phone, services-offered referencing this tenant catalog) + 9 new items across the four clusters (Location/Visit logistics: parking, transit, weekend walk-in wait; Booking: deposit, cancellation policy; Pricing: enhancement-set range, nail-art extra — qualitative ranges only per D-29, NO exact prices in prose; Service specifics: choose a technician, gel polish on manicures). FR first (source-of-truth, vous, D-21), then native-quality idiomatic EN (D-24) with identical item count and key structure per locale (Pitfall 2). Author maily, charlesbourg, rivieres with their OWN distinct facts but shared brand voice (D-23). With ~9 base + 13 tenant the union is ~22 per tenant per locale (>=20 floor with buffer, D-04). Commit fr+en per tenant together.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts -t "D-05" &amp;&amp; bun test src/config/seo/seo-parity.test.ts -t "faq"</automated>
  </verify>
  <done>All 3 live tenants have >=13 per-tenant items per locale in intent order with real per-tenant facts; D-05 floor test GREEN (union >=20); per-tenant faq fr/en parity GREEN; charlesbourg/rivieres carry NO maily facts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire /faq render + JSON-LD to the getTenantFaq union (F-01)</name>
  <files>src/app/[lang]/faq/page.tsx</files>
  <action>
    In src/app/[lang]/faq/page.tsx replace the current `dict.faq.items` usage (call site ~line 30 JSON-LD, ~line 39 Accordion). Call `getTenantFaq(lang)` once to get the merged intent-ordered union, then pass the SAME array to BOTH `faqPageGraph(union)` (JSON-LD, keeping mainEntity count == union length for F-01) and `<Accordion items={union} />` (render). Import getTenantFaq from `../get-tenant-faq`. Keep the existing PageHeader (the FAQ page is OUT of CONTENT-01 answer-block scope per RESEARCH §h1 audit — its PageHeader h1 stays). Do NOT modify src/lib/seo.ts faqPageGraph (D-28) — it already maps item.a to acceptedAnswer.text and ignores item.link, keeping schema text clean (D-30). force-dynamic page resolves the tenant at request time.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts -t "faqPageGraph" &amp;&amp; bun run build</automated>
  </verify>
  <done>/faq renders getTenantFaq union and feeds the same union to faqPageGraph; F-01 count test GREEN; src/lib/seo.ts untouched; PageHeader h1 retained on FAQ; build green.</done>
</task>

</tasks>

<verification>
- `bun test src/config/schema-invariants.test.ts -t "D-05"` GREEN (every live tenant union >=20 per locale)
- `bun test src/config/seo/seo-parity.test.ts` GREEN (base dict parity + per-tenant faq parity + no drift)
- Base dict faq has no brand names / no tenant facts (grep gate in Task 1)
- charlesbourg/rivieres FAQ carries their own facts only (no Carrefour Beauport leak)
- `/faq` JSON-LD mainEntity count == rendered Accordion item count (F-01)
- `bun run build` succeeds (D-05 guard still unwired until 03-05; tests already GREEN)
</verification>

<success_criteria>
Success Criterion 2 TRUE: >=20 distinct Q&A per tenant per locale, FR keys identical to EN. Cross-tenant FAQ defect fixed. /faq + JSON-LD fed by one intent-ordered union (F-01). All Wave-3 FAQ tests GREEN; build green.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author to git | FAQ content is author-written JSON committed to git; no user input path |
| tenant isolation | Each tenant FAQ resolves from its own faq.{locale}.json; cross-tenant leakage is the defect this slice fixes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Spoofing | cross-tenant FAQ fact leak | mitigate | Per-tenant faq.{locale}.json resolved by tenant.id; grep + D-05 tests confirm charlesbourg/rivieres carry no maily facts |
| T-03-06 | Information disclosure | XSS via FAQ answer/link text | accept | Author-written JSON; React escapes string output; faqPageGraph emits plain a text (RESEARCH Security Domain) |
| T-03-SC | Tampering | npm installs | mitigate | No new packages this phase (RESEARCH Standard Stack) — no install task |
</threat_model>

<output>
Create `.planning/phases/03-content-depth/03-03-SUMMARY.md` when done
</output>
