---
phase: 03-content-depth
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/config/tenants/ongles-maily/seo.fr.json
  - src/config/tenants/ongles-maily/seo.en.json
  - src/config/tenants/ongles-charlesbourg/seo.fr.json
  - src/config/tenants/ongles-charlesbourg/seo.en.json
  - src/config/tenants/ongles-rivieres/seo.fr.json
  - src/config/tenants/ongles-rivieres/seo.en.json
  - src/app/[lang]/page.tsx
  - src/app/[lang]/services/page.tsx
  - src/app/[lang]/services/[slug]/page.tsx
  - src/app/[lang]/locations/page.tsx
requirements: [CONTENT-01]
autonomous: true

must_haves:
  truths:
    - "Home, services index, every services/[slug], and locations index lead with a self-contained direct-answer <AnswerBlock> rendered FIRST in main, ABOVE the hero/marketing copy (D-17)"
    - "Each route answerBlock is non-empty with >= 2 answer-first sentences per tenant per locale, authored as DISTINCT answerBlock/answerHeading fields in seo.{locale}.json (D-08, NOT reused from meta.*)"
    - "Exactly one <h1> per page: the AnswerBlock carries the h1, the hero/PageHeader heading demotes to <h2> (D-19) — no double-h1"
    - "answerBlock prose carries at most one inline link and no CTA chrome (D-16); replaces the old dictionary intro copy so there is a single lead per page (D-15)"
    - "Hero image stays priority/eager — text paints first, LCP unchanged (D-20)"
  artifacts:
    - path: "src/config/tenants/ongles-maily/seo.fr.json"
      provides: "Real answerBlock + answerHeading copy for home, services-index, 4 service slugs, locations"
      contains: "answerBlock"
    - path: "src/components/AnswerBlock.tsx"
      provides: "Server component carrying the page h1 (created in 03-02)"
      contains: "AnswerBlock"
    - path: "src/app/[lang]/page.tsx"
      provides: "AnswerBlock first in main; hero h1 demoted to h2"
      contains: "AnswerBlock"
  key_links:
    - from: "src/app/[lang]/page.tsx"
      to: "src/config/tenants/ongles-maily/seo.fr.json"
      via: "getSeo(lang).meta.homeAnswerBlock/homeAnswerHeading feed AnswerBlock as the single page h1"
      pattern: "homeAnswer"
    - from: "src/app/[lang]/services/[slug]/page.tsx"
      to: "src/components/AnswerBlock.tsx"
      via: "renders getSeo().services[service.id].answerBlock first in main, demotes detail h1 to h2"
      pattern: "AnswerBlock"
---

<objective>
Vertical content slice B — direct-answer blocks (CONTENT-01). After this slice every key page (home, services index, each services/[slug], locations index) opens with a self-contained factual paragraph BEFORE any marketing copy, carrying the page h1. Author the DISTINCT answerBlock/answerHeading fields per tenant in seo.{locale}.json (D-08 — NOT reused from meta.*), wire `<AnswerBlock>` first in main on all 4 page types (D-17), and reshuffle headings so each page has exactly one h1 (D-19).

This slice is gated by the D-11 presence/length guard tests authored RED in 03-01: once copy lands, those flip GREEN (guard wiring into the build happens in 03-05). The base stub keys (created in 03-01) define the canonical key set; this plan fills real copy into the 3 live tenants. FR authored first (D-21), EN native-quality (D-24), committed together (Pitfall 5).

Purpose: Make Success Criterion 1 TRUE (every key page leads with a >=2-sentence direct-answer paragraph above marketing copy, single h1).
Output: Real answerBlock/answerHeading copy in 3 live tenants (fr+en); AnswerBlock wired first-in-main on 4 page types; hero/PageHeader h1 demoted to h2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-depth/03-CONTEXT.md
@.planning/phases/03-content-depth/03-RESEARCH.md

@src/components/AnswerBlock.tsx
@src/app/[lang]/page.tsx
@src/app/[lang]/services/page.tsx
@src/app/[lang]/services/[slug]/page.tsx
@src/app/[lang]/locations/page.tsx
@src/components/PageHeader.tsx
@src/config/tenants/ongles-maily/seo.fr.json

# RESEARCH §h1 audit: home h1 at page.tsx line ~105 (dict.hero.taglineLead) demote
# to h2; services index + locations use <PageHeader> (renders h1) — replace with
# <AnswerBlock> or demote PageHeader heading to h2; service [slug] inline h1 at
# line ~91 (<h1>{d.title}</h1>) demote to h2. The page fragment is FIRST inside
# layout.tsx <main> — putting AnswerBlock first in the fragment satisfies D-17.
# D-08 OVERRIDES research open-question #1: answerBlock/answerHeading are DISTINCT
# new fields, NOT meta.*Title/meta.*Description. D-14 gives the canonical question
# per page. D-29: qualitative price ranges only — no exact prices in prose.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author answerBlock + answerHeading copy in 3 live tenants (D-08/D-14)</name>
  <files>src/config/tenants/ongles-maily/seo.fr.json, src/config/tenants/ongles-maily/seo.en.json, src/config/tenants/ongles-charlesbourg/seo.fr.json, src/config/tenants/ongles-charlesbourg/seo.en.json, src/config/tenants/ongles-rivieres/seo.fr.json, src/config/tenants/ongles-rivieres/seo.en.json</files>
  <action>
    Fill the stub answerBlock/answerHeading keys (added to base + tenant seo files in 03-01) with REAL copy for each live tenant (ongles-maily, ongles-charlesbourg, ongles-rivieres), both fr and en. Per D-08 these are DISTINCT fields — do NOT copy from meta.*Title/meta.*Description. Fields to fill (field nesting per D-10 — per-service blocks nest under the existing seo.services[slug] entries; index-level blocks under meta.*Answer*; the locations block under the new top-level locations key): meta.homeAnswerHeading + meta.homeAnswerBlock; meta.servicesAnswerHeading + meta.servicesAnswerBlock; each services[slug] (pose-ongles, remplissage, soins-mains, soins-pieds) answerHeading + answerBlock; locations.answerHeading + locations.answerBlock. Factual overlap with FAQ answers is allowed but vary the wording — no verbatim duplication (D-27). Author per D-14 canonical questions: home = what/where is {salon} + services in {city}; services index = what services + price range; service detail = what is {service}, price, duration; locations = where are the salons + how to book. Each answerBlock: >=2 answer-first sentences (D-11/D-22, ~15-25 words each, one fact each, stand-alone-quotable), 2-4 sentences / ~60 words ceiling (D-12 soft). Use that tenant OWN facts (city, neighborhood) — read each tenant config; never hardcode another tenant facts. Qualitative price ranges only (D-29 — no exact numbers in prose). Shared brand voice (D-23); FR vous first (D-21), native-quality EN (D-24); align facts with Phase 2 JSON-LD by authoring discipline only (D-28 — do NOT touch src/lib/seo.ts). Keep fr/en key structure identical (Pitfall 5). Note: a separate inline link inside answerBlock is optional (<=1, D-16); if used, model it as the answerBlock string plus a dedicated answerLink {href,label} key mirrored fr/en, OR omit links entirely for blocks — pick one approach and apply it consistently across all tenants so parity holds.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts -t "D-11" &amp;&amp; bun test src/config/seo/seo-parity.test.ts</automated>
  </verify>
  <done>All 3 live tenants have non-empty answerHeading/answerBlock (>=2 sentences) for home, services-index, 4 service slugs, locations, both locales; D-11 presence test GREEN; seo-parity GREEN; no exact prices in answerBlock prose; src/lib/seo.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AnswerBlock first-in-main on home + services index (D-17/D-19)</name>
  <files>src/app/[lang]/page.tsx, src/app/[lang]/services/page.tsx</files>
  <action>
    HOME (src/app/[lang]/page.tsx): import AnswerBlock from @/components/AnswerBlock. Render it as the FIRST element of the returned fragment (above the hero section) so it is first in layout.tsx main (D-17). Feed heading={seo.meta.homeAnswerHeading}, text={seo.meta.homeAnswerBlock} (and link if the consistent approach from Task 1 uses one). Demote the existing hero h1 (line ~105, dict.hero.taglineLead) to <h2> so the AnswerBlock h1 is the single page h1 (D-19). Keep the hero image priority/eager (D-20 — do not change image loading). Retire the old inline intro per D-15 (single lead per page — the hero tagline becomes h2 subordinate copy, no second intro paragraph competing as the lead).

    SERVICES INDEX (src/app/[lang]/services/page.tsx): render <AnswerBlock heading={seo.meta.servicesAnswerHeading} text={seo.meta.servicesAnswerBlock}> FIRST in the fragment. Replace the <PageHeader> h1 (it renders h1 at PageHeader line 15) — either remove PageHeader or pass it a prop/wrapper so its heading is <h2>; the cleanest path per RESEARCH §h1 audit is replacing PageHeader with AnswerBlock and moving any retained intro to non-h1 copy. Ensure exactly one h1 on the page (D-19, D-15).

    getSeo(lang) is already called on both pages (home) / add it on services index if absent. force-dynamic resolves per tenant at request time.
  </action>
  <verify>
    <automated>bun run build &amp;&amp; node -e "const fs=require('fs'); for(const p of ['src/app/[lang]/page.tsx','src/app/[lang]/services/page.tsx']){const c=fs.readFileSync(p,'utf8'); const n=(c.match(/&lt;h1/g)||[]).length; if(n>1){console.error(p,'has',n,'h1');process.exit(1)} if(!/AnswerBlock/.test(c)){console.error(p,'missing AnswerBlock');process.exit(1)}} console.log('home+services: single h1 + AnswerBlock ok')"</automated>
  </verify>
  <done>Home and services index render AnswerBlock first in main; hero h1 demoted to h2; PageHeader h1 on services index removed/demoted; exactly one h1 per page; hero image still priority; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire AnswerBlock first-in-main on service detail + locations (D-17/D-19)</name>
  <files>src/app/[lang]/services/[slug]/page.tsx, src/app/[lang]/locations/page.tsx</files>
  <action>
    SERVICE DETAIL (src/app/[lang]/services/[slug]/page.tsx): the page already resolves service via serviceBySlug and reads seo.services[service.id]. Render <AnswerBlock heading={s.answerHeading} text={s.answerBlock}> FIRST in the returned fragment. Demote the existing detail <h1>{d.title}</h1> (line ~91) to <h2> so AnswerBlock carries the single h1 (D-19). Keep the service hero image priority unchanged (D-20).

    LOCATIONS (src/app/[lang]/locations/page.tsx): render <AnswerBlock heading={seo.locations.answerHeading} text={seo.locations.answerBlock}> FIRST in the fragment (single index-level block per RESEARCH §D-09 — one canonical "where + how to book" answer, not per-location). Replace/demote the <PageHeader> h1 to <h2> so AnswerBlock is the single h1. Add getSeo(lang) if not already called. The per-location SalonCards (from getStoreConfig) stay below the block unchanged.

    On both pages: at most one inline link in the block (D-16), no CTA chrome; exactly one h1 (D-19); single lead per page (D-15).
  </action>
  <verify>
    <automated>bun run build &amp;&amp; node -e "const fs=require('fs'); for(const p of ['src/app/[lang]/services/[slug]/page.tsx','src/app/[lang]/locations/page.tsx']){const c=fs.readFileSync(p,'utf8'); const n=(c.match(/&lt;h1/g)||[]).length; if(n>1){console.error(p,'has',n,'h1');process.exit(1)} if(!/AnswerBlock/.test(c)){console.error(p,'missing AnswerBlock');process.exit(1)}} console.log('service+locations: single h1 + AnswerBlock ok')"</automated>
  </verify>
  <done>Service detail and locations render AnswerBlock first in main; detail h1 and PageHeader h1 demoted to h2; exactly one h1 per page; images still priority; locations uses single index-level block; build green.</done>
</task>

</tasks>

<verification>
- `bun test src/config/schema-invariants.test.ts -t "D-11"` GREEN (every route answerBlock non-empty, >=2 sentences, per tenant per locale)
- `bun test src/config/seo/seo-parity.test.ts` GREEN (answerBlock/answerHeading key parity fr/en, no drift — Pitfall 5)
- Each of the 4 page types renders AnswerBlock FIRST in main, above hero/marketing copy (D-17)
- Exactly one h1 per page across all 4 page types (grep gate in Tasks 2/3 — no double-h1, Pitfall 3)
- Hero/service images remain priority/eager (D-20 LCP unchanged)
- `src/lib/seo.ts` untouched (D-28); `content.{locale}.json` untouched (D-26)
- `bun run build` succeeds (D-11 guard still unwired until 03-05; tests already GREEN)
</verification>

<success_criteria>
Success Criterion 1 TRUE: home, services index, every services/[slug], and locations index lead with a self-contained >=2-sentence direct-answer paragraph BEFORE marketing copy, each page exactly one h1. answerBlock/answerHeading are distinct authored fields (D-08). All Wave-3 answer-block tests GREEN; build green.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author to git | answerBlock copy is author-written JSON committed to git; no user input path |
| build to deploy | D-11 presence/length tests gate content; the guard (wired in 03-05) aborts deploy on a missing/short block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | double-h1 / heading hierarchy regression | mitigate | Grep gate asserts exactly one h1 per page across all 4 page types (Pitfall 3) |
| T-03-08 | Information disclosure | XSS via answerBlock/link text | accept | Author-written JSON; React escapes string output by default (RESEARCH Security Domain) |
| T-03-09 | Denial | LCP/CWV regression from block above hero | mitigate | Text paints first with no image dependency; hero image stays priority/eager (D-20) |
| T-03-SC | Tampering | npm installs | mitigate | No new packages this phase (RESEARCH Standard Stack) — no install task |
</threat_model>

<output>
Create `.planning/phases/03-content-depth/03-04-SUMMARY.md` when done
</output>
