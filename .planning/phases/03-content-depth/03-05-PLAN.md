---
phase: 03-content-depth
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-03", "03-04"]
files_modified:
  - src/config/schema-invariants.ts
  - src/config/schema-invariants.test.ts
  - .planning/phases/03-content-depth/03-UAT.md
requirements: [CONTENT-01, CONTENT-02]
autonomous: false

must_haves:
  truths:
    - "checkFaqFloor() and checkAnswerBlockPresence() are now CALLED from validateSchemaInvariants/assertSchemaInvariants so a shortfall aborts next build and the Dokploy deploy (D-05/D-11)"
    - "All Phase 3 guard tests are GREEN (content from 03-03/03-04 satisfies >=20 floor and >=2-sentence presence) and bun run build passes with guards live"
    - "A deliberately-broken fixture (empty answerBlock or <20 FAQ) makes the wired guard FAIL the build, proving the gate bites (D-05/D-11)"
    - "Success Criterion 4 is encoded as a documented manual-UAT step (ChatGPT/Perplexity spot-check), not an automated test"
    - "schema-invariants.ts stays alias-free (relative JSON imports only) after wiring (Pitfall 1)"
  artifacts:
    - path: "src/config/schema-invariants.ts"
      provides: "checkFaqFloor + checkAnswerBlockPresence wired into validateSchemaInvariants (build-blocking)"
      contains: "checkFaqFloor"
    - path: ".planning/phases/03-content-depth/03-UAT.md"
      provides: "Manual UAT script for Success Criterion 4 + the full automated coverage map"
      contains: "Perplexity"
  key_links:
    - from: "src/config/schema-invariants.ts"
      to: "next.config.ts"
      via: "assertSchemaInvariants (now including FAQ floor + answer-block presence) runs under PHASE_PRODUCTION_BUILD"
      pattern: "assertSchemaInvariants"
    - from: "src/config/schema-invariants.ts"
      to: "src/config/tenants/ongles-maily/faq.fr.json"
      via: "checkFaqFloor reads per-tenant faq JSON via relative path (alias-free)"
      pattern: "faq\\.fr\\.json"
---

<objective>
Activate the build gate and verify the phase end-to-end. Wire the two guard functions (`checkFaqFloor` D-05, `checkAnswerBlockPresence` D-11) — implemented unwired in 03-01 — into `validateSchemaInvariants()` so a content shortfall aborts `next build` and the Dokploy deploy. Prove the gate bites with a fail-fixture, run the full suite + build green, then author the manual-UAT script that encodes Success Criterion 4 (ChatGPT/Perplexity spot-check) and the complete Nyquist coverage map.

This is the closing flip: 03-01 wrote the guards RED-and-unwired specifically so the build stayed green while content landed across 03-03/03-04. Now that content satisfies the floors, wiring the guards makes them GREEN-and-live. A human checkpoint confirms the manual AI-citation spot-check (Criterion 4) before the phase closes.

Purpose: Make the build-enforced Truths (D-05, D-11) live, confirm Success Criteria 1-3 are automated-GREEN, and capture Criterion 4 as a documented manual UAT.
Output: Wired+live guards; fail-fixture proof; full suite + build green; 03-UAT.md with coverage map + manual spot-check script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-depth/03-CONTEXT.md
@.planning/phases/03-content-depth/03-RESEARCH.md

@src/config/schema-invariants.ts
@src/config/schema-invariants.test.ts

# validateSchemaInvariants() is the aggregator called by assertSchemaInvariants()
# (which next.config.ts invokes under PHASE_PRODUCTION_BUILD). Wiring = push
# checkFaqFloor()/checkAnswerBlockPresence() results into the errors array. Keep
# schema-invariants.ts alias-free — the guards already import per-tenant JSON via
# relative paths (added 03-01). Do NOT import getTenantFaq here (Pitfall 1/4).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire FAQ-floor + answer-block guards into the build gate (D-05/D-11)</name>
  <files>src/config/schema-invariants.ts, src/config/schema-invariants.test.ts</files>
  <action>
    In src/config/schema-invariants.ts, inside validateSchemaInvariants(), push the results of checkFaqFloor() and checkAnswerBlockPresence() into the errors array (alongside the existing checkIdUniqueness/checkFaqCompleteness and the per-tenant loop). These functions were implemented in 03-01 but intentionally NOT called; now call them so assertSchemaInvariants() — invoked by next.config.ts under PHASE_PRODUCTION_BUILD — aborts the build/deploy on any shortfall (D-05 >=20 floor, D-11 answer-block >=2 sentences per required route per tenant per locale). Keep the module alias-free: the guards already import per-tenant faq/seo JSON via relative paths; do NOT add @/ imports and do NOT import getTenantFaq (Pitfall 1/4). Add/adjust a test in schema-invariants.test.ts asserting validateSchemaInvariants() returns ZERO errors now that real content from 03-03/03-04 is in place (the previously-RED D-05/D-11 describe blocks are now GREEN).
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts &amp;&amp; bun run build</automated>
  </verify>
  <done>checkFaqFloor + checkAnswerBlockPresence are called from validateSchemaInvariants; full schema-invariants test suite GREEN (D-05/D-11 now pass with real content); bun run build succeeds with guards live; module still alias-free.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the gate bites (fail-fixture) then restore</name>
  <files>src/config/schema-invariants.test.ts</files>
  <action>
    Add a test that proves the wired guard FAILS on a shortfall WITHOUT permanently breaking real content. Two acceptable approaches — pick one: (a) a unit test that constructs an in-memory tenant FAQ array of length < FAQ_FLOOR and an empty answerBlock string and asserts checkFaqFloor/checkAnswerBlockPresence (or their pure inner predicates) return a non-empty SchemaInvariantError[]; or (b) a test that monkeypatches/injects a deliberately-empty fixture and asserts validateSchemaInvariants() reports the expected error, then verifies real content yields zero errors. Do NOT mutate the committed JSON content files. The point is to PROVE the gate is not a no-op: a sub-floor count or empty block produces an error that would abort the build. Confirm afterward that real content still yields zero errors.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts -t "gate" &amp;&amp; bun test src/</automated>
  </verify>
  <done>A test proves checkFaqFloor/checkAnswerBlockPresence return errors on a sub-floor/empty fixture (gate bites); committed content unchanged; full suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Author 03-UAT.md — Criterion 4 manual spot-check + coverage map</name>
  <files>.planning/phases/03-content-depth/03-UAT.md</files>
  <action>
    Create .planning/phases/03-content-depth/03-UAT.md documenting (1) the Nyquist coverage map: Success Criterion 1 -> D-11 answerBlock presence test (schema-invariants.test.ts); Criterion 2 -> D-05 FAQ floor test + per-tenant faq parity (schema-invariants.test.ts, seo-parity.test.ts); Criterion 3 -> seo-parity.test.ts GREEN; D-05/D-11/F-01/F-02 -> their named tests; Criterion 4 -> THIS manual step. (2) The manual UAT script for Criterion 4: for each live tenant, query ChatGPT and Perplexity for "[salon name] services [city]" using that tenant real name+city; record whether the answer reproduces at least one factual sentence drawn from the site answer blocks or FAQ; capture a pass/fail + the quoted sentence. Note this is a retrieval/indexing-dependent check (may lag deploy) and is a documented manual gate, not an automated test (Criterion 4 wording). Include the exact tenant names/cities to query (read from each tenant config). List the live URLs to spot-check post-deploy (Dokploy webhook deploy — verify on the live www host per project memory).
  </action>
  <verify>
    <human-check>03-UAT.md lists all 3 tenants with name+city queries, the pass/fail capture format, and maps all 4 success criteria to either an automated test or this manual step</human-check>
  </verify>
  <done>03-UAT.md exists with the full automated-coverage map and a runnable manual spot-check script for Criterion 4 across all 3 live tenants on both ChatGPT and Perplexity.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — Phase 3 end-to-end + Criterion-4 spot-check</name>
  <action>Human confirms rendered output, live build guards, tenant-correct FAQ, single-h1 answer blocks, and the manual AI-citation spot-check before the phase closes. No code change — verification only.</action>
  <what-built>Phase 3 complete: live FAQ-floor + answer-block build guards (D-05/D-11), >=20 FAQ per tenant per locale with the cross-tenant defect fixed, direct-answer blocks leading all 4 page types with single h1, and 03-UAT.md with the manual AI-citation spot-check.</what-built>
  <how-to-verify>
    1. Run `bun test src/` — all GREEN (D-05, D-11, F-01, F-02, parity).
    2. Run `bun run build` — succeeds with guards live (PHASE_PRODUCTION_BUILD path).
    3. Run `bun dev`, visit home / services / a services/[slug] / locations for at least 2 tenants (set TENANT env per tenant): confirm each page LEADS with the answer paragraph above the hero, exactly one h1 (DevTools/axe), and /faq shows >=20 tenant-correct Q&A (charlesbourg/rivieres show THEIR facts, not Ongles Maily).
    4. After deploy, run the 03-UAT.md Criterion-4 spot-check on ChatGPT + Perplexity for at least one tenant and record the result.
  </how-to-verify>
  <resume-signal>Type "approved" once build is green, pages lead with the answer block (single h1), FAQ is tenant-correct and >=20, and the Criterion-4 spot-check is recorded; or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- `bun test src/` full suite GREEN (D-05 floor, D-11 presence, F-01 count, F-02 parity, sentence-splitter, loader merge)
- `bun run build` succeeds with guards WIRED and live (validateSchemaInvariants includes the two new checks)
- Fail-fixture test proves the gate aborts on a sub-floor count / empty answerBlock (not a no-op)
- schema-invariants.ts remains alias-free (no @/, no getTenantFaq import) — verified by successful Docker-equivalent build
- 03-UAT.md maps all 4 success criteria (1-3 automated, 4 manual) and gives the per-tenant spot-check script
- Human checkpoint approves rendered output + Criterion-4 result
</verification>

<success_criteria>
All four phase Success Criteria verifiably TRUE: (1) answer blocks lead all 4 page types with single h1 [D-11 test + visual], (2) >=20 FAQ per tenant per locale FR=EN [D-05 test + parity], (3) seo-parity GREEN [parity test], (4) AI-citation spot-check recorded [03-UAT.md manual]. Guards live and build-blocking; build green.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build to deploy | The now-live guard runs in next.config.ts under PHASE_PRODUCTION_BUILD; a content shortfall aborts the Dokploy deploy |
| author to git | All content and the UAT doc are author-written and committed; no user input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial | wired guard breaks a previously-passing deploy | mitigate | Wiring happens only after 03-03/03-04 content satisfies the floors; full build verified green before this plan closes |
| T-03-11 | Tampering | alias creep into schema-invariants.ts during wiring | mitigate | No @/ imports added; guards reuse relative-path JSON; Docker-equivalent bun run build is the proof gate (Pitfall 1) |
| T-03-SC | Tampering | npm installs | mitigate | No new packages this phase (RESEARCH Standard Stack) — no install task |
</threat_model>

<output>
Create `.planning/phases/03-content-depth/03-05-SUMMARY.md` when done
</output>
