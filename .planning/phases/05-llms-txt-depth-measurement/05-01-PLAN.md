---
phase: 05-llms-txt-depth-measurement
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [LLMS-01, LLMS-02, LOCAL-01, CONV-01, CONV-02, MEAS-01, MEAS-02]
files_modified:
  - src/config/types.ts
  - src/config/tenants/ongles-maily/site.ts
  - src/config/tenants/ongles-charlesbourg/site.ts
  - src/config/tenants/ongles-rivieres/site.ts
  - src/config/tenants/template/site.ts
  - src/config/schema-invariants.ts
  - src/config/schema-invariants.test.ts
  - src/lib/gtag.ts
  - src/lib/gtag.test.ts
user_setup: []

must_haves:
  truths:
    - "TenantSite type carries ga4MeasurementId and llmsDescription fields; tsc --noEmit passes with all tenants populated (placeholder llmsDescription/ID for now)"
    - "Four new guard functions (checkLlmsDepth, checkLlmsLeak, checkGA4IdPresent, checkNapConsistency) exist, exported, alias-free, and UNWIRED from validateSchemaInvariants() so next build stays green"
    - "gtagEvent() + ga4Events emitters exist as pure browser helpers with no React/side-effects"
    - "RED tests for all four guards and gtag exist and pin the contracts (some assert current GREEN where placeholder data already satisfies, some assert RED until 05-05 content lands)"
  artifacts:
    - path: "src/config/types.ts"
      provides: "ga4MeasurementId + llmsDescription on TenantSite"
      contains: "llmsDescription"
    - path: "src/config/schema-invariants.ts"
      provides: "checkLlmsDepth, checkLlmsLeak, checkGA4IdPresent, checkNapConsistency (exported, unwired)"
      contains: "export function checkLlmsLeak"
    - path: "src/lib/gtag.ts"
      provides: "gtagEvent() + ga4Events conversion emitters"
      exports: ["gtagEvent", "ga4Events"]
    - path: "src/lib/gtag.test.ts"
      provides: "gtag event name + param shape tests"
    - path: "src/config/schema-invariants.test.ts"
      provides: "tests for the 4 new guards"
  key_links:
    - from: "src/config/schema-invariants.ts"
      to: "src/config/index.ts"
      via: "TENANT_REGISTRY iteration (already imported at top of file)"
      pattern: "TENANT_REGISTRY"
    - from: "src/config/schema-invariants.ts"
      to: "site.llmsDescription / site.ga4MeasurementId / location"
      via: "reads cfg.site + cfg.location (alias-free, no @/ imports)"
      pattern: "cfg\\.site\\.(llmsDescription|ga4MeasurementId)"
---

<objective>
Lay the typed, tested foundation that all three Phase-5 vertical slices wire into: the two net-new `TenantSite` fields, the four build-guard check functions (kept UNWIRED so `next build` stays green until owner content lands in 05-05), and the pure `gtag.ts` event helper.

Purpose: Phase 5 is three disjoint slices (llms.txt, measurement, conversion) that all consume the same new config fields and guard machinery. Building that substrate once — type-first, test-first — lets 05-02 and 05-03 run in parallel without colliding.
Output: `TenantSite` fields, 4 exported-but-unwired guards, `src/lib/gtag.ts`, and RED/locked tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llms-txt-depth-measurement/05-CONTEXT.md
@.planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md
@.planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates: `ga4MeasurementId` + `llmsDescription` (TenantSite fields, src/config/types.ts); `checkLlmsDepth`, `checkLlmsLeak`, `checkGA4IdPresent`, `checkNapConsistency` (guards, src/config/schema-invariants.ts); `gtagEvent` + `ga4Events` (src/lib/gtag.ts). Downstream plans add: `WebVitalsReporter`, `ConsentBanner`, `FloatingCTAButtons` client island (05-03/05-04), and the new test files src/app/llms.txt/route.test.ts, src/app/[lang]/layout.test.tsx, src/components/WebVitalsReporter.test.tsx.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TenantSite fields + populate placeholders across all tenants</name>
  <files>src/config/types.ts, src/config/tenants/ongles-maily/site.ts, src/config/tenants/ongles-charlesbourg/site.ts, src/config/tenants/ongles-rivieres/site.ts, src/config/tenants/template/site.ts</files>
  <read_first>
    - src/config/types.ts (TenantSite type — add fields after the existing `contact` / before `nav` cluster; keep both REQUIRED non-optional per RESEARCH Pattern 6)
    - src/config/tenants/ongles-charlesbourg/site.ts (the `as const` literal shape every tenant follows; add the two fields)
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md §"Pattern 6: Config Field Additions"
  </read_first>
  <behavior>
    - Test: `bunx tsc --noEmit` passes after every tenant site.ts has both new fields.
    - Test: TenantSite type exposes `ga4MeasurementId: string` and `llmsDescription: string` (both required).
  </behavior>
  <action>
    In `src/config/types.ts`, add two REQUIRED fields to `TenantSite` per D-L03 (llmsDescription) and D-M01 (GA4 ID): `ga4MeasurementId: string` (JSDoc: empty string = no analytics for this tenant; warning-level guard) and `llmsDescription: string` (JSDoc: hand-authored llms.txt intro, ≥200 unique words, no other tenant's city/landmark). In each tenant `site.ts` (maily, charlesbourg, rivieres, template) add `ga4MeasurementId: ""` (empty placeholder — real `G-XXXXXXXXXX` collected in 05-05 human-verify) and a placeholder `llmsDescription: ""` (real ≥200-word prose authored in 05-05). Do NOT add either field to `SiteSectionSchema` (Supabase override surface) — mirror the canonicalUrl I-01 exclusion so remote content injection is impossible (RESEARCH anti-pattern). Keep `as const` on each literal.
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` exits 0.
    - `grep -c 'llmsDescription' src/config/types.ts` ≥ 1 and `ga4MeasurementId` present in TenantSite.
    - All four tenant site.ts files contain both `ga4MeasurementId` and `llmsDescription` keys.
    - Neither field appears in `SiteSectionSchema`.
  </acceptance_criteria>
  <done>TenantSite has both required fields; all tenants populate them (placeholders); tsc clean; fields excluded from Supabase override schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gtag.ts pure event helper + RED tests</name>
  <files>src/lib/gtag.ts, src/lib/gtag.test.ts</files>
  <read_first>
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md §"Pattern 4: Conversion Event Emission" (gtagEvent signature, ga4Events shape, event names)
    - .planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md (MEAS-01 row: gtagEvent() names/params correct)
    - src/lib/format.ts (existing pure-helper style in src/lib for conventions)
  </read_first>
  <behavior>
    - Test: gtagEvent('book_online_click', {...}) calls window.gtag('event','book_online_click',{...}) exactly once when window.gtag is defined.
    - Test: gtagEvent is a no-op (no throw) when window.gtag is undefined.
    - Test: ga4Events.bookOnlineClick(loc) emits name 'book_online_click' with event_category 'conversion' and salon_location param.
    - Test: ga4Events.callClick / contactFormSubmit / directionsClick emit the M-03 event names with documented params.
  </behavior>
  <action>
    Create `src/lib/gtag.ts` per D-M03/M-04: declare the global `Window.gtag?` + `Window.dataLayer?` augmentation; export pure `gtagEvent(name, params?)` that calls `window.gtag?.('event', name, params)`; export `ga4Events` const object with the four M-03 emitters — `bookOnlineClick(location)` → `book_online_click` (primary key event, event_category 'conversion'), `callClick(phone)` → `call_click`, `contactFormSubmit()` → `contact_form_submit`, `directionsClick(location)` → `directions_click` (event_category 'engagement'). No React imports, no module-level side effects (this file must be importable from both client islands and bun:test). Write `src/lib/gtag.test.ts` mocking `window.gtag` (assign a spy, assert call args; reset between tests) covering all behaviors above. These tests are GREEN immediately (pure logic, no owner data).
  </action>
  <verify>
    <automated>bun test src/lib/gtag.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/lib/gtag.test.ts` passes (all behavior cases green).
    - gtag.ts has no `import ... from 'react'` and no top-level executable side effects.
    - `ga4Events` exports exactly the four M-03 emitters with the exact event names `book_online_click`, `call_click`, `contact_form_submit`, `directions_click`.
  </acceptance_criteria>
  <done>gtag.ts pure helper + ga4Events exist with the four M-03 event names; test file green; no React/side-effect deps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Four new schema-invariants guards (UNWIRED) + RED tests</name>
  <files>src/config/schema-invariants.ts, src/config/schema-invariants.test.ts</files>
  <read_first>
    - src/config/schema-invariants.ts (existing guard pattern: `err()`, `EXCLUDED_TENANTS`, `countWords()`, `checkOfferTypes` analog; the alias-free / no-process.env header constraints; `validateSchemaInvariants()` composition — DO NOT wire the new guards in yet)
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md §"Pattern 7: Schema Invariants Extensions" (exact algorithm for all four)
    - src/config/tenants/ongles-charlesbourg/location.ts (the `location` object the NAP guard compares against: phone, address.street/city/postalCode, hoursSpec)
    - .planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md (LLMS-01/02, LOCAL-01 test rows + Wave 0 list)
  </read_first>
  <behavior>
    - Test: checkLlmsDepth() returns an error for a tenant whose llmsDescription is empty or <200 words (currently RED for all live tenants — placeholders are empty; this is expected until 05-05).
    - Test: checkLlmsLeak() returns an error when one tenant's llmsDescription contains another tenant's city or landmark string (use a crafted fixture / direct-call assertion).
    - Test: checkGA4IdPresent() returns a (warning-class) error for a tenant with empty ga4MeasurementId.
    - Test: checkNapConsistency() returns zero errors for current tenants (site.contact === location for phone/address/postalCode; hoursSpec block count matches) and returns an error under a mismatched fixture.
  </behavior>
  <action>
    Add four exported, alias-free functions to `schema-invariants.ts` exactly per RESEARCH Pattern 7, reusing existing `countWords()`, `err()`, `EXCLUDED_TENANTS`, and `TENANT_REGISTRY` (already imported): `checkLlmsDepth()` (llmsDescription present + `countWords()` ≥ 200 per non-template tenant, invariant tag 'LLMS-02'); `checkLlmsLeak()` (builds per-tenant `[contact.address.city, contact.landmark]` signal map, flags any tenant whose llmsDescription contains another tenant's signal case-insensitively, tag 'LLMS-01'); `checkGA4IdPresent()` (flags empty `ga4MeasurementId`, tag 'MEAS-01' — see N-02 note: this is collected separately as a WARNING in 05-05, NOT a hard build-fail); `checkNapConsistency()` (asserts `cfg.site.contact.phone/address.street/city/postalCode` equal `cfg.location.*` and `site.hours.length === location.hoursSpec.length`, tag 'NAP-01'). Use RELATIVE imports only; no `process.env`, no network. Do NOT add any of them to `validateSchemaInvariants()` yet — 05-05 wires them once owner content lands (mirror the 03/04 unwired-until-activation pattern in this same file). Extend `schema-invariants.test.ts` with the behavior cases above; for checkLlmsLeak/checkNapConsistency negative cases, call the function or assert against a crafted in-test fixture rather than mutating real config.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/config/schema-invariants.test.ts` passes (RED-expectation tests assert the guard *reports* the shortfall, e.g. `expect(checkLlmsDepth().length).toBeGreaterThan(0)` while placeholders are empty).
    - `grep -c 'checkLlmsDepth\|checkLlmsLeak\|checkGA4IdPresent\|checkNapConsistency' src/config/schema-invariants.ts` shows all four functions defined and exported.
    - `next build` still green: the four new guards do NOT appear inside `validateSchemaInvariants()` body. Verify: extract the `validateSchemaInvariants` function body and confirm none of the four new names are called there (grep the call-site region, filtered for non-comment lines).
    - `bunx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Four guards exist, exported, alias-free, tested, and UNWIRED; build stays green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time config → schema guard | tenant config literals feed the build-blocking guard; a leak/empty field must be caught here, not at runtime |
| client island → window.gtag | gtag helper runs in the browser; only emits analytics events, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | llmsDescription cross-tenant leak | mitigate | checkLlmsLeak() guard (wired build-blocking in 05-05) flags another tenant's city/landmark in any tenant body |
| T-05-02 | Tampering | llmsDescription via Supabase admin override | mitigate | field excluded from SiteSectionSchema (.strict() rejects it) — mirror canonicalUrl I-01 exclusion |
| T-05-03 | Information Disclosure | gtag analytics data | accept | only behavioral event names/params sent to GA4; gated by Consent Mode v2 (05-03); no PII in event params |
| T-05-SC | Tampering | npm/bun installs | mitigate | no new packages this plan — next/web-vitals + next/script are built-in (no bun install needed) |
</threat_model>

<verification>
- `bunx tsc --noEmit` clean
- `bun test src/lib/gtag.test.ts src/config/schema-invariants.test.ts` green
- `bun test src/` full unit suite green (no regressions vs ~357 baseline; 11 pre-existing Playwright failures excluded)
- `validateSchemaInvariants()` does NOT reference the four new guards (build stays green)
</verification>

<success_criteria>
TenantSite carries both new required fields; all tenants populate placeholders; four guards exist, exported, alias-free, tested, UNWIRED; gtag.ts pure helper green; full unit suite green; no new dependencies added.
</success_criteria>

<output>
Create `.planning/phases/05-llms-txt-depth-measurement/05-01-SUMMARY.md` when done.
</output>
