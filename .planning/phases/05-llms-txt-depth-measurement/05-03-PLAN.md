---
phase: 05-llms-txt-depth-measurement
plan: 03
type: tdd
wave: 2
depends_on: ["05-01"]
autonomous: true
requirements: [MEAS-01, MEAS-02]
files_modified:
  - src/app/[lang]/layout.tsx
  - src/app/[lang]/layout.test.tsx
  - src/components/ConsentBanner.tsx
  - src/components/WebVitalsReporter.tsx
  - src/components/WebVitalsReporter.test.tsx
  - src/config/base/content.fr.json
  - src/config/base/content.en.json
user_setup:
  - service: google-analytics-4
    why: "Per-tenant GA4 property + measurement ID (M-01). Code emits the script only when ga4MeasurementId is non-empty; real IDs collected in 05-05."
    dashboard_config:
      - task: "Create one GA4 property per tenant; AI-Assistant channel group is native (June 2026); add custom Perplexity regex channel"
        location: "GA4 Admin -> Data Streams / Channel groups (manual admin step, documented in 05-05, not a code deliverable)"

must_haves:
  truths:
    - "When ga4MeasurementId is non-empty, layout renders GA4 Script tags: inline beforeInteractive Consent-Mode-v2 denied-by-default init + afterInteractive gtag.js src + gtag-config (M-01/M-02)"
    - "When ga4MeasurementId is empty, NO gtag.js Script is emitted (tenant-in-transition fallback)"
    - "ConsentBanner client island shows on first visit, persists choice in localStorage, fires gtag consent update (analytics_storage granted, ad_storage denied) on accept (M-02, Law 25)"
    - "WebVitalsReporter client island calls window.gtag event for INP/LCP/CLS via useReportWebVitals from next/web-vitals (M-05)"
    - "Consent banner FR/EN strings live in config/base/content.{fr,en}.json under an identical consent key (locale parity)"
  artifacts:
    - path: "src/components/ConsentBanner.tsx"
      provides: "Law-25 consent banner client island"
      contains: "use client"
    - path: "src/components/WebVitalsReporter.tsx"
      provides: "useReportWebVitals to gtag reporter"
      contains: "useReportWebVitals"
    - path: "src/app/[lang]/layout.test.tsx"
      provides: "GA4 Script presence/absence test"
  key_links:
    - from: "src/app/[lang]/layout.tsx"
      to: "site.ga4MeasurementId"
      via: "resolved from getStoreConfig(); gates Script emission + passed to islands"
      pattern: "ga4MeasurementId"
    - from: "src/components/WebVitalsReporter.tsx"
      to: "window.gtag"
      via: "useReportWebVitals callback emits gtag event"
      pattern: "gtag"
    - from: "src/components/ConsentBanner.tsx"
      to: "config/base/content.*.json consent key"
      via: "dict.consent strings (composeDictionary)"
      pattern: "consent"
---

<objective>
Vertical slice 2 — measurement. Stand up GA4 (currently none exists) via hand-rolled next/script in the layout with Consent-Mode-v2 denied-by-default (Quebec Law 25), a ConsentBanner client island, and a WebVitalsReporter client island reporting real-user INP/CWV to the same per-tenant GA4 property. After this slice, with a real measurement ID, GA4 DebugView shows page views + CWV events once a user accepts consent.

Purpose: MEAS-01/02 give the before/after baseline for every content phase. Law 25 makes denied-by-default the only defensible default for this Quebec business.
Output: layout Script wiring + ConsentBanner + WebVitalsReporter + consent dict keys + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llms-txt-depth-measurement/05-CONTEXT.md
@.planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md
@.planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md
@.planning/phases/05-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates: ConsentBanner (src/components/ConsentBanner.tsx), WebVitalsReporter (src/components/WebVitalsReporter.tsx), src/app/[lang]/layout.test.tsx (new), src/components/WebVitalsReporter.test.tsx (new), and a consent key in config/base/content.{fr,en}.json. It consumes ga4MeasurementId (05-01) and gtagEvent/window.gtag (05-01). No new npm packages — next/script and next/web-vitals are Next.js 16.2.6 built-ins.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WebVitalsReporter island + RED test (next/web-vitals to gtag)</name>
  <files>src/components/WebVitalsReporter.tsx, src/components/WebVitalsReporter.test.tsx</files>
  <read_first>
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md, Pattern 3 Web Vitals Reporter (useReportWebVitals from next/web-vitals — NOT external web-vitals; metric shape name/id/value/rating; CLS x1000 scaling; non_interaction true)
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-report-web-vitals.md (API + metric object shape — read before writing, Next 16.2.6 is non-standard)
    - src/components/PopupHost.tsx (existing use-client island pattern in this codebase)
    - .planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md (MEAS-02 row + Wave 0 WebVitalsReporter.test.tsx)
  </read_first>
  <behavior>
    - Test: when a metric name INP with id/value/rating fires, the reporter calls window.gtag event INP with value, event_label id, non_interaction true, metric_rating rating.
    - Test: a CLS metric is reported with value scaled x1000 and rounded.
    - Test: when measurementId is empty, no gtag call is made.
  </behavior>
  <action>
    Create src/components/WebVitalsReporter.tsx as a use-client island per RESEARCH Pattern 3: import useReportWebVitals from next/web-vitals (built-in — do NOT add the external web-vitals package; this avoids the dual-lockfile trap), accept a measurementId string prop, and in the metric callback call window.gtag with event, metric.name, and params value (Math.round of metric.value, scaled x1000 when metric.name is CLS), event_label metric.id, non_interaction true, metric_rating metric.rating; return null. Bail early when measurementId is empty. Write WebVitalsReporter.test.tsx that mocks useReportWebVitals (capture the callback) and window.gtag (spy), invokes the captured callback with INP and CLS fixtures, and asserts the gtag call args. These tests are GREEN immediately.
  </action>
  <verify>
    <automated>bun test src/components/WebVitalsReporter.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - bun test src/components/WebVitalsReporter.test.tsx green.
    - WebVitalsReporter imports from next/web-vitals (built-in), not from web-vitals.
    - No web-vitals entry added to package.json (confirm grep count of the package name in package.json is 0).
  </acceptance_criteria>
  <done>WebVitalsReporter island emits gtag CWV events via built-in next/web-vitals; test green; no external dep added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConsentBanner island + FR/EN dict keys (Law 25 Consent Mode v2)</name>
  <files>src/components/ConsentBanner.tsx, src/config/base/content.fr.json, src/config/base/content.en.json</files>
  <read_first>
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md, Pattern 2 Consent Banner Client Island (localStorage key ga4_consent, accept fires gtag consent update analytics_storage granted / ad_storage denied, decline path, show-on-first-visit) and Open Questions Q4 (FR/EN copy)
    - src/app/[lang]/dictionaries.ts + src/app/[lang]/compose-dictionary.ts (how config/base/content.*.json feeds the composed dict — consent strings go HERE, NOT src/dictionaries/*.json which is type-only; runtime dynamic routes need the runtime dict)
    - src/config/base/content.fr.json (top-level keys nav, cta, hero, ... — add a sibling consent key with identical structure to content.en.json)
    - src/components/PopupHost.tsx (localStorage persistence + use-client pattern reference)
  </read_first>
  <behavior>
    - Test (or manual UAT): on first visit (no localStorage key) the banner renders; on accept it sets localStorage ga4_consent to accepted and calls gtag consent update with analytics_storage granted and ad_storage denied; on decline it sets declined and hides.
    - Locale parity: content.fr.json and content.en.json both have a consent key with identical sub-keys.
  </behavior>
  <action>
    Create src/components/ConsentBanner.tsx as a use-client island per RESEARCH Pattern 2: props locale and measurementId; useEffect reads localStorage key ga4_consent — if accepted immediately call gtag consent update (analytics_storage granted, ad_storage denied), if unset setShow true; accept() persists accepted + fires the consent update, decline() persists declined; bail when measurementId empty (no analytics so no banner needed). Read FR/EN copy from the composed dict new consent key — do NOT hardcode strings in the component. Add the consent key to BOTH config/base/content.fr.json and config/base/content.en.json with identical structure (message, accept, decline, ariaLabel) using the Law-25-minimal copy from RESEARCH Q4. ad_storage stays denied even on accept (Law 25 — advertising cookies require separate opt-in).
  </action>
  <verify>
    <automated>bunx tsc --noEmit && grep -q '"consent"' src/config/base/content.fr.json && grep -q '"consent"' src/config/base/content.en.json</automated>
  </verify>
  <acceptance_criteria>
    - ConsentBanner.tsx starts with the use-client directive and renders no string literals (all copy from dict.consent).
    - content.fr.json and content.en.json both contain a consent object with identical sub-keys (the inline grep+tsc verify passes; no external script needed).
    - bunx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>ConsentBanner island reads consent dict, persists choice, fires Consent-Mode-v2 update on accept; FR/EN consent keys at parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire GA4 Script blocks + island mounts into layout.tsx + RED layout test</name>
  <files>src/app/[lang]/layout.tsx, src/app/[lang]/layout.test.tsx</files>
  <read_first>
    - src/app/[lang]/layout.tsx (Server Component, force-dynamic; getStoreConfig resolves site; FloatingCTA + PopupHost + CustomCodeHost already mounted in body — add Script blocks + islands here)
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md, Pattern 1 GA4 Load in Server Component Layout (two Script blocks: inline beforeInteractive consent default with id gtag-consent-init, then afterInteractive gtag.js src + inline gtag-config; id prop REQUIRED on inline scripts for React 19; onLoad/onReady FORBIDDEN in Server Components)
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/script.md (Script strategies + Server Component constraints — read before writing, Next 16.2.6 is non-standard, next/third-parties does NOT exist)
    - .planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md (MEAS-01 layout test row + Wave 0 layout.test.tsx)
  </read_first>
  <behavior>
    - Test: rendering the layout with a non-empty ga4MeasurementId produces an inline beforeInteractive consent-init Script (id gtag-consent-init), a gtag.js src Script pointing at googletagmanager.com with the id, and a gtag-config Script.
    - Test: rendering with an empty ga4MeasurementId emits NO gtag.js src Script (no googletagmanager.com reference).
    - Test: ConsentBanner and WebVitalsReporter are mounted in the layout.
  </behavior>
  <action>
    Edit src/app/[lang]/layout.tsx per RESEARCH Pattern 1. Resolve measurementId from the already-resolved site (site.ga4MeasurementId). In the body, add: (1) inline Script id gtag-consent-init strategy beforeInteractive containing the dataLayer init + gtag function + gtag consent default with all denied (ad_storage, analytics_storage, ad_user_data, ad_personalization) and wait_for_update 500; (2) only when measurementId is truthy, the afterInteractive gtag.js src Script (googletagmanager.com/gtag/js?id=measurementId) plus inline Script id gtag-config strategy afterInteractive with gtag js new Date and gtag config measurementId. Mount ConsentBanner (locale + measurementId) and WebVitalsReporter (measurementId) as siblings near the existing FloatingCTA. Do NOT use onLoad/onReady (Server Component forbidden). Do NOT import next/third-parties (absent). Keep import Script from next/script. Write layout.test.tsx asserting the three behaviors (render the layout output for both empty and non-empty measurementId, inspecting the produced Script elements / markup). Note CSP follow-up: googletagmanager.com must be allowlisted in script-src/connect-src if a CSP is later added (none deployed today — documented in threat model, not a code deliverable here).
  </action>
  <verify>
    <automated>bun test src/app/[lang]/layout.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - bun test src/app/[lang]/layout.test.tsx green for both the non-empty (Script present) and empty (Script absent) measurementId cases.
    - grep count of next/third-parties in layout.tsx is 0; grep count of onLoad in layout.tsx is 0.
    - Inline Script blocks carry id props gtag-consent-init and gtag-config.
    - bun test src/ full unit suite green (no regressions); bunx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Layout emits Consent-Mode-v2 init + conditional gtag.js Scripts and mounts both islands; layout test green for present/absent ID; no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser user -> GA4 (Google) | behavioral analytics data leaves to Google; Law 25 requires consent gating |
| third-party script origin | googletagmanager.com script loaded into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | GA4 behavioral data exfiltration pre-consent | mitigate | Consent Mode v2 denied-by-default (analytics_storage denied) until banner accept; cookieless pings only pre-consent per Google spec |
| T-05-03-02 | Tampering | localStorage consent value | accept | value only ever compared to accepted/declined string literals; no eval, no injection surface |
| T-05-03-03 | Information Disclosure | CSP gap for googletagmanager.com | accept | no CSP currently deployed; documented follow-up to allowlist *.googletagmanager.com in script-src/connect-src when CSP lands |
| T-05-03-SC | Tampering | npm/bun installs | mitigate | NO new packages — next/script + next/web-vitals are Next.js 16.2.6 built-ins; no bun install needed |
</threat_model>

<verification>
- bun test src/components/WebVitalsReporter.test.tsx + src/app/[lang]/layout.test.tsx green
- consent FR/EN key parity passes
- bun test src/ full unit suite green (no regressions)
- bunx tsc --noEmit clean
- Manual (UAT, 05-05): GA4 DebugView shows events on accept; DevTools Network shows /collect carrying en=INP
</verification>

<success_criteria>
GA4 loads via hand-rolled next/script with Consent-Mode-v2 denied-by-default; ConsentBanner + WebVitalsReporter mount in layout; Script absent when no measurement ID; consent dict at FR/EN parity; tests green; no new deps.
</success_criteria>

<output>
Create `.planning/phases/05-llms-txt-depth-measurement/05-03-SUMMARY.md` when done.
</output>
