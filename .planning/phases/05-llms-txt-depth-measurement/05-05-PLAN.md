---
phase: 05-llms-txt-depth-measurement
plan: 05
type: execute
wave: 4
depends_on: ["05-02", "05-03", "05-04"]
autonomous: false
requirements: [LLMS-01, LLMS-02, LOCAL-01, MEAS-01]
files_modified:
  - src/config/tenants/ongles-maily/site.ts
  - src/config/tenants/ongles-charlesbourg/site.ts
  - src/config/tenants/ongles-rivieres/site.ts
  - src/config/schema-invariants.ts
  - docs/nap-reference.md
  - .planning/phases/05-llms-txt-depth-measurement/05-UAT.md
user_setup:
  - service: google-analytics-4
    why: "Owner must create one GA4 property per tenant and supply each G-XXXXXXXXXX measurement ID (M-01); owner/copywriter must supply each tenant's >=200-word llmsDescription prose (L-03/LLMS-02)."
    env_vars: []
    dashboard_config:
      - task: "Create GA4 properties (3 tenants); configure native AI-Assistant channel group + custom Perplexity regex channel"
        location: "GA4 Admin -> Property + Channel groups"

must_haves:
  truths:
    - "Each tenant site.ts has its real ga4MeasurementId (or intentionally-empty for a tenant in transition) and a >=200-word llmsDescription with no cross-tenant city/landmark"
    - "checkLlmsDepth, checkLlmsLeak, checkNapConsistency are WIRED into validateSchemaInvariants() (build-blocking); checkGA4IdPresent is collected as a WARNING (build proceeds)"
    - "next build (production) passes with all guards live; the build aborts on a seeded leak/short-description fixture (gate-bites proof)"
    - "docs/nap-reference.md lists the canonical per-tenant NAP for external GBP/directory alignment (N-02)"
    - "05-UAT.md documents the manual GA4 DebugView / DevTools / mobile-CTA / AI-citation checks (success criteria 3,4,5,6)"
  artifacts:
    - path: "docs/nap-reference.md"
      provides: "per-tenant canonical NAP reference (N-02)"
      contains: "NAP Reference"
    - path: ".planning/phases/05-llms-txt-depth-measurement/05-UAT.md"
      provides: "manual verification checklist for the 6 ROADMAP success criteria"
    - path: "src/config/schema-invariants.ts"
      provides: "validateSchemaInvariants() wiring the new guards"
      contains: "checkLlmsLeak"
  key_links:
    - from: "src/config/schema-invariants.ts validateSchemaInvariants()"
      to: "checkLlmsDepth / checkLlmsLeak / checkNapConsistency"
      via: "errors.push(...check...()) in the function body (build-blocking)"
      pattern: "errors\\.push\\(\\.\\.\\.checkLlms"
    - from: "next.config.ts assertSchemaInvariants()"
      to: "validateSchemaInvariants()"
      via: "already wired build guard (Phase 2) now covers the new checks"
      pattern: "assertSchemaInvariants"
---

<objective>
Integration + activation. Collect the owner-supplied content (GA4 measurement IDs + the >=200-word per-tenant llmsDescription prose) via blocking human-verify checkpoints, populate the tenant configs, then flip the three content guards (checkLlmsDepth, checkLlmsLeak, checkNapConsistency) to build-blocking in validateSchemaInvariants() with checkGA4IdPresent as a warning. Produce the N-02 NAP reference doc and the 05-UAT.md manual checklist, then run the full verification + production build to prove the gates bite.

Purpose: This is the activation gate that turns the placeholder fields from 05-01 into real, guarded content — and the point where llms.txt finally exceeds 200 words per tenant (LLMS-02). The guards stay unwired through 05-01..05-04 so the build stays green during parallel development; this plan closes the loop.
Output: populated tenant configs, wired guards, NAP doc, UAT doc, green production build.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llms-txt-depth-measurement/05-CONTEXT.md
@.planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md
@.planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md
@.planning/phases/05-01-SUMMARY.md
@.planning/phases/05-02-SUMMARY.md
@.planning/phases/05-03-SUMMARY.md
@.planning/phases/05-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates: docs/nap-reference.md (N-02), .planning/phases/05-llms-txt-depth-measurement/05-UAT.md. It modifies the three tenant site.ts files (real ga4MeasurementId + llmsDescription prose) and schema-invariants.ts (wires checkLlmsDepth/checkLlmsLeak/checkNapConsistency into validateSchemaInvariants, checkGA4IdPresent as warning). Consumes guards from 05-01, route from 05-02, layout from 05-03.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Collect owner-supplied GA4 IDs + per-tenant llmsDescription prose</name>
  <what-built>
    The TenantSite.ga4MeasurementId and TenantSite.llmsDescription fields (05-01) and the GA4 load + llms.txt body (05-02/05-03) are in place but the owner-only content is still placeholder. This checkpoint collects it.
  </what-built>
  <how-to-verify>
    Provide, per tenant (ongles-maily, ongles-charlesbourg, ongles-rivieres):
    1. The GA4 measurement ID in the form G-XXXXXXXXXX (created at GA4 Admin -> Data Streams). If a tenant has no property yet, reply "skip" for that tenant — its ga4MeasurementId stays empty (no analytics emitted; checkGA4IdPresent warns, build proceeds).
    2. A hand-authored llmsDescription paragraph of AT LEAST 200 words describing THAT salon only — its services, hours, location/landmark, and booking path — written so an AI engine can quote it standalone. It must NOT mention any other tenant's city or landmark (no "Carrefour Beauport" in charlesbourg/rivieres, etc.). A drafting prompt/template per tenant is provided in 05-RESEARCH Pattern 6.
    Confirm the GA4 channel-group admin step separately (native AI-Assistant channel + custom Perplexity regex) — this is GA4-console config, not code.
  </how-to-verify>
  <action>Blocking human-verify checkpoint: pause execution and collect the owner-supplied GA4 measurement IDs and per-tenant >=200-word llmsDescription prose described in how-to-verify. Do not auto-generate this content. Resume only after the owner provides it.</action>
  <verify><human-check>Owner has supplied 3 GA4 IDs (or explicit skip) and 3 llmsDescription paragraphs.</human-check></verify>
  <done>GA4 IDs and llmsDescription prose collected from the owner, ready to paste into tenant configs in Task 2.</done>
  <resume-signal>Paste the 3 GA4 IDs (or "skip") and the 3 llmsDescription paragraphs, then type "approved".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Populate tenant configs + wire guards build-blocking + gate-bites proof</name>
  <files>src/config/tenants/ongles-maily/site.ts, src/config/tenants/ongles-charlesbourg/site.ts, src/config/tenants/ongles-rivieres/site.ts, src/config/schema-invariants.ts</files>
  <read_first>
    - src/config/schema-invariants.ts (validateSchemaInvariants() body — wire the new checks the same way Phase 3/4 wired checkFaqFloor/checkWordCount; note the WARNING handling for checkGA4IdPresent per RESEARCH Pattern 7 + Open Question 3)
    - src/config/tenants/ongles-charlesbourg/site.ts (the ga4MeasurementId + llmsDescription placeholders from 05-01 to fill)
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md, Pattern 7 (wiring) + Open Question 3 (checkGA4IdPresent as warning, not hard-fail)
    - src/config/schema-invariants.test.ts (the RED depth/leak tests from 05-01 turn GREEN once real content lands; add a gate-bites fixture test)
  </read_first>
  <action>
    Paste the approved GA4 IDs and llmsDescription prose into each tenant site.ts (Task 1 output), replacing the 05-01 placeholders. Then wire the guards into validateSchemaInvariants() in schema-invariants.ts: add errors.push of checkLlmsDepth(), checkLlmsLeak(), and checkNapConsistency() to the build-blocking error array (mirroring the existing checkFaqFloor/checkWordCount/checkRoutePresence wiring). For checkGA4IdPresent(): collect its result SEPARATELY and emit it as a console warning (build proceeds) rather than pushing into the throwing error array — implement the warning path per RESEARCH Open Question 3 (a tenant in transition may legitimately have no GA4 ID). Add a gate-bites test to schema-invariants.test.ts proving a seeded short (<200-word) or cross-tenant-leaking llmsDescription fixture makes the relevant guard report an error (so the build would abort). The 05-01 depth/leak tests that were RED now go GREEN with real content.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts src/app/llms.txt/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - bun test src/config/schema-invariants.test.ts src/app/llms.txt/route.test.ts green (depth/leak now satisfied by real content; gate-bites fixture proves the guard reports a violation).
    - validateSchemaInvariants() body calls checkLlmsDepth, checkLlmsLeak, checkNapConsistency (grep the function body, non-comment lines).
    - checkGA4IdPresent is NOT pushed into the throwing error array (it is handled as a warning) — verify it does not appear in the build-blocking errors.push cluster.
    - Each non-skipped tenant llmsDescription has >=200 words (checkLlmsDepth passes); no cross-tenant city/landmark (checkLlmsLeak passes).
  </acceptance_criteria>
  <done>Real owner content populated; depth/leak/NAP guards build-blocking; GA4-ID guard warning-only; gate-bites proven; relevant tests green.</done>
</task>

<task type="auto">
  <name>Task 3: NAP reference doc (N-02) + full verify + production build</name>
  <files>docs/nap-reference.md, .planning/phases/05-llms-txt-depth-measurement/05-UAT.md</files>
  <read_first>
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md, Pattern 9 (N-02 NAP reference format — produced once by reading TENANT_REGISTRY, NOT auto-generated per build) + the NAP surfaces list
    - src/config/tenants/ongles-maily/location.ts + ongles-charlesbourg/location.ts + ongles-rivieres/location.ts (canonical NAP values to transcribe)
    - .planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md, Manual-Only Verifications table (the exact UAT steps to put in 05-UAT.md)
    - .planning/phases/04-net-new-pages (04-UAT.md as the format template for the UAT doc)
  </read_first>
  <action>
    Create docs/nap-reference.md per RESEARCH Pattern 9: one section per tenant with the canonical Name, Address, Phone, Landmark, and Hours transcribed from each tenant's config (location.ts / site.ts) — this is the reference a human uses to align GBP/directory profiles (N-02). State that off-site profile creation is out of scope. Create 05-UAT.md listing the manual verifications from 05-VALIDATION (GA4 DebugView shows the 4 conversion events on consent accept [SC-3]; DevTools Network /collect carries en=INP, INP P75 in GA4 [SC-6/MEAS-02]; mobile sticky CTA above fold at scroll 0 + persists [SC-4/CONV-01]; trust/price anchors above fold on home + service [SC-5/CONV-02]; per-tenant llms.txt has no cross-tenant facts + >200 words via curl on each host [SC-1/SC-2]; GA4 AI-Assistant + Perplexity-regex channel groups configured [SC-3 admin]) with per-criterion pass/fail boxes. Then run the full unit suite and a production build to prove all guards pass end-to-end.
  </action>
  <verify>
    <automated>bun test src/ && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - docs/nap-reference.md exists with a section per live tenant (Name/Address/Phone/Landmark/Hours) and the out-of-scope note.
    - 05-UAT.md exists covering all 6 ROADMAP success criteria with manual steps + pass/fail boxes.
    - bun test src/ full unit suite green.
    - bun run build (production) completes green with all four-guard wiring live (no schema-invariant abort) — this exercises assertSchemaInvariants via next.config.ts.
  </acceptance_criteria>
  <done>NAP reference + UAT docs written; full suite + production build green with guards live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT — GA4 events, CWV, mobile CTA, above-fold signals, llms.txt</name>
  <what-built>
    GA4 with Consent Mode v2, WebVitals reporting, 4 conversion events, above-fold trust/price signals, and per-tenant leak-free deepened llms.txt are all live. This checkpoint verifies the manual-only success criteria (3,4,5,6) and the llms.txt content (1,2) on real browsers/hosts.
  </what-built>
  <how-to-verify>
    Follow 05-UAT.md:
    1. SC-1/2 (llms.txt): curl https://<each-tenant-host>/llms.txt -> confirm only that tenant's facts, no "Carrefour Beauport"/other-tenant landmarks, and >200 words.
    2. SC-3 (GA4): accept the consent banner -> click book / call / submit contact form / directions -> confirm book_online_click, call_click, contact_form_submit, directions_click in GA4 DebugView. Confirm AI-Assistant + Perplexity channel groups exist in GA4 admin.
    3. SC-4 (CONV-01): DevTools mobile emulation (iPhone) on the home page -> FloatingCTA visible at scroll 0 and after scrolling.
    4. SC-5 (CONV-02): mobile home + service pages -> experience badge + price-from anchor in the first viewport (stars only if review data exists).
    5. SC-6 (MEAS-02): DevTools Network -> confirm a /collect request carries en=INP; INP/LCP/CLS events visible in GA4.
  </how-to-verify>
  <action>Blocking human-verify checkpoint: pause and walk through the 05-UAT.md manual steps in how-to-verify (llms.txt curl per host, GA4 DebugView for the 4 conversion events, mobile CTA above-fold, above-fold trust/price signals, CWV /collect en=INP). Do not mark the phase complete until the owner confirms all 6 ROADMAP success criteria pass.</action>
  <verify><human-check>All 6 ROADMAP Phase-5 success criteria confirmed passing by the owner against 05-UAT.md.</human-check></verify>
  <done>All 6 ROADMAP success criteria verified on real browsers/hosts; phase ready to mark complete.</done>
  <resume-signal>Type "approved" if all 6 criteria pass, or describe the failing criterion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner-supplied prose -> config -> build guard | hand-authored llmsDescription must pass leak + depth guards before it can ship |
| build-time guard -> deploy | a leak/short-description aborts next build and the Dokploy deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Information Disclosure | owner pastes another tenant's landmark into llmsDescription | mitigate | checkLlmsLeak() now build-blocking — aborts the build before deploy |
| T-05-05-02 | Spoofing | thin/empty llmsDescription degrades AI-citation quality | mitigate | checkLlmsDepth() build-blocking enforces >=200 words per non-skipped tenant |
| T-05-05-03 | Tampering | NAP drift between site.contact and location | mitigate | checkNapConsistency() build-blocking asserts the two NAP objects match per tenant |
| T-05-05-SC | Tampering | npm/bun installs | mitigate | no new packages this plan; if any dep was added upstream, run bun install to sync both lockfiles (dual-lockfile trap) before build |
</threat_model>

<verification>
- bun test src/ full unit suite green
- bun run build (production) green with all guards live (gate exercised via next.config.ts assertSchemaInvariants)
- gate-bites: seeded short/leaking fixture makes the guard report a violation (proven in Task 2 test)
- Manual UAT (Task 4): all 6 ROADMAP success criteria pass
</verification>

<success_criteria>
Owner content populated; depth/leak/NAP guards build-blocking (GA4-ID warning-only); production build green; NAP reference + UAT docs written; all 6 ROADMAP success criteria verified.
</success_criteria>

<output>
Create `.planning/phases/05-llms-txt-depth-measurement/05-05-SUMMARY.md` when done.
</output>
