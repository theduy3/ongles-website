---
phase: 05-llms-txt-depth-measurement
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
autonomous: true
requirements: [LLMS-01, LLMS-02, LOCAL-01]
files_modified:
  - src/app/llms.txt/route.ts
  - src/app/llms.txt/route.test.ts
files_owned_note: "Reads (does not modify) src/config/schema-invariants.ts guards added by 05-01."

must_haves:
  truths:
    - "llms.txt served for charlesbourg/rivieres contains zero 'Carrefour Beauport' / Beauport / ongles-maily-only strings — every fact resolved from getStoreConfig() (LLMS-01)"
    - "llms.txt links use site.canonicalUrl (not site.url) for both FR canonical and EN-equivalent sections (L-02)"
    - "llms.txt body includes service list + CAD price ranges, hours, booking path (generated), Phase-4 page links, hand-authored llmsDescription intro (L-03/L-04/L-05/L-06)"
    - "A per-tenant llms.txt route test proves no cross-tenant leak + canonicalUrl usage + section presence for all 3 tenants"
  artifacts:
    - path: "src/app/llms.txt/route.ts"
      provides: "leak-free, deepened, canonicalUrl-based per-tenant llms.txt"
      contains: "site.canonicalUrl"
    - path: "src/app/llms.txt/route.test.ts"
      provides: "per-tenant body assertions (no leak, canonicalUrl, sections)"
  key_links:
    - from: "src/app/llms.txt/route.ts"
      to: "src/lib/store-config.ts"
      via: "getStoreConfig() resolves site + services per tenant"
      pattern: "getStoreConfig"
    - from: "src/app/llms.txt/route.ts"
      to: "site.llmsDescription / site.canonicalUrl"
      via: "intro prose + link base from config (not hardcoded, not site.url)"
      pattern: "site\\.(canonicalUrl|llmsDescription)"
---

<objective>
Vertical slice 1 — the AI-discovery layer. Fix the cross-tenant leak in `src/app/llms.txt/route.ts` (L-01) and rebuild the body so every fact is config-resolved and the output exceeds 200 unique words per tenant: generated service/price/hours/booking facts, hand-authored `site.llmsDescription` intro, Phase-4 page links (L-04), `site.canonicalUrl` base (L-02), FR-canonical-then-EN-equivalents ordering (L-05). After this slice, a real AI crawler hitting `/llms.txt` on the charlesbourg host gets only charlesbourg facts.

Purpose: The current route hardcodes "Carrefour Beauport, Québec City" prose, which is factually wrong (a lie) for charlesbourg and rivieres — the headline bug of LLMS-01.
Output: rewritten `route.ts` + a new per-tenant `route.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llms-txt-depth-measurement/05-CONTEXT.md
@.planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md
@.planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md
@.planning/phases/05-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates: src/app/llms.txt/route.test.ts (new). It modifies src/app/llms.txt/route.ts. It consumes the `llmsDescription` field from 05-01 (still placeholder-empty until 05-05 — the test asserts structure + no-leak + canonicalUrl, NOT the ≥200-word content which is gated by 05-05's checkLlmsDepth wiring).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test — per-tenant llms.txt body (no leak, canonicalUrl, sections)</name>
  <files>src/app/llms.txt/route.test.ts</files>
  <read_first>
    - src/app/llms.txt/route.ts (current GET handler — the leak at lines 10 site.url, 15 blockquote, 31 landmark)
    - src/lib/store-config.ts (getStoreConfig() return shape: site, services, locations)
    - src/config/tenants/ongles-charlesbourg/site.ts + src/config/tenants/ongles-rivieres/site.ts (the city/landmark strings that must NOT appear in each other's body: "Carrefour Beauport"/"Beauport", "Carrefour Charlesbourg", "Centre Les Rivières"/"Trois-Rivières")
    - .planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md (LLMS-01/02 route test row + Wave 0 entry for route.test.ts)
    - src/config/get-tenant-faq.ts (pattern for how a test drives tenant resolution under bun:test, if helpful)
  </read_first>
  <behavior>
    - Test (per tenant): GET() body contains site.name and uses site.canonicalUrl as the link host (assert the FR base `${canonicalUrl}/fr` and EN base `${canonicalUrl}/en` appear).
    - Test (per tenant): body contains NO other tenant's city/landmark string (charlesbourg body must not contain "Beauport" or "Carrefour Beauport"; rivieres body must not contain "Charlesbourg"; etc.).
    - Test (per tenant): body contains a Services section with each service id and a CAD price token, a Hours section, a Booking link, and the Phase-4 links (/tarifs or /pricing, the 4 comparison slugs, the borough near-me slug).
    - Test: body does NOT contain the literal runtime `site.url` host when it differs from canonicalUrl (guards the L-02 fix).
  </behavior>
  <action>
    Create `src/app/llms.txt/route.test.ts` driving the route's `GET()` for each live tenant (resolve via the project's existing tenant-selection mechanism used by other src tests — set the active tenant the same way `get-tenant-faq.test.ts` / store-config tests do; do not invent a new harness). Assert the four behaviors above. The cross-tenant no-leak assertion is the spine of LLMS-01: build the other-tenant signal list from each tenant's `contact.address.city` + `contact.landmark` and assert `body` excludes them case-insensitively. These tests start RED against the current hardcoded route (charlesbourg body currently leaks "Carrefour Beauport"). Do NOT assert the ≥200-word content floor here — that is owner prose gated by 05-05.
  </action>
  <verify>
    <automated>bun test src/app/llms.txt/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and runs; before Task 2 it FAILS (RED) on at least the charlesbourg/rivieres no-leak + canonicalUrl assertions.
    - No new tenant-resolution harness invented — uses the existing pattern from sibling src tests.
  </acceptance_criteria>
  <done>RED per-tenant route test exists asserting no-leak, canonicalUrl base, and required sections for all 3 tenants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite route.ts — config-resolved, deepened, canonicalUrl-based (GREEN)</name>
  <files>src/app/llms.txt/route.ts</files>
  <read_first>
    - src/app/llms.txt/route.ts (replace the hardcoded body)
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md §"Pattern 5: llms.txt Leak Fix" (exact deepened body structure: blockquote→llmsDescription, base→canonicalUrl, landmark from config, generated services/hours, FR + English-equivalents sections, comparison slugs from COMPARISONS)
    - src/lib/comparisons.ts (COMPARISONS registry — source the comparison slugs per Pitfall 5 rather than hardcoding; map FR + EN slugs)
    - src/lib/i18n.ts (defaultLocale)
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/route-handlers (or the route-handler doc) — Next.js 16.2.6 GET handler + Response conventions before editing
  </read_first>
  <behavior>
    - Test: all assertions in route.test.ts pass for all 3 tenants (GREEN).
  </behavior>
  <action>
    Rewrite `GET()` in `route.ts` per RESEARCH Pattern 5 and L-01..L-06. (1) L-02: `const base = ` + canonicalUrl + `/` + defaultLocale; `enBase` = canonicalUrl + `/en`. (2) L-01/L-03: replace the hardcoded blockquote with `> ${site.llmsDescription}` and the "directions to Carrefour Beauport" string with `site.contact.landmark`. (3) L-06: generate `## Contact & Location` (address/landmark/phone/email/languages), `## Hours` from `site.hours` (days + opens–closes), `## Services & Pricing (CAD)` from `services[]` (`$price–$priceTo CAD`), `## Booking` (`${base}${site.booking}`). (4) L-04/L-05: `## Key Pages (FR — canonical)` linking /services, /tarifs, /faq, /contact, /book-online, /locations (with landmark), the borough near-me slug (last entry of `site.routes` or the per-tenant borough slug), and `## Comparaisons (FR)` for the 4 FR comparison slugs; then `## English Equivalents` linking the EN routes/comparison slugs via `enBase`. Source comparison slugs from `COMPARISONS` (Pitfall 5) — do not hardcode strings that could 404. ZERO hardcoded city/landmark/tenant literals anywhere in the body (L-01 non-negotiable). Keep the `text/plain; charset=utf-8` Response. Note: `llmsDescription` is still placeholder-empty until 05-05; the body structure + links are what this task delivers GREEN now.
  </action>
  <verify>
    <automated>bun test src/app/llms.txt/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/app/llms.txt/route.test.ts` GREEN for all 3 tenants.
    - `grep -nE 'Carrefour Beauport|Beauport|Québec City' src/app/llms.txt/route.ts | grep -v '^[0-9]*://' ` returns no hardcoded-prose hits (the only allowed occurrence of any city name is via `${site.contact...}` interpolation, not a literal). Verify no literal city/landmark string remains.
    - `grep -c 'site.url' src/app/llms.txt/route.ts` is 0 (replaced by canonicalUrl, L-02).
    - `bunx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>route.ts emits a leak-free, config-resolved, canonicalUrl-based, deepened body; route test green for all tenants; no literal tenant strings remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request → llms.txt route | request-time tenant resolution; output must contain ONLY the resolved tenant's facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | cross-tenant fact leak in llms.txt body | mitigate | all facts from getStoreConfig(); route test asserts no other tenant's city/landmark appears; checkLlmsLeak() build guard (wired 05-05) |
| T-05-02-02 | Spoofing | wrong host in AI-crawler-facing links | mitigate | use site.canonicalUrl (stable, non-Supabase-overridable I-01), not runtime site.url (L-02) |
| T-05-02-SC | Tampering | npm/bun installs | mitigate | no new packages — route uses existing getStoreConfig + COMPARISONS |
</threat_model>

<verification>
- `bun test src/app/llms.txt/route.test.ts` green (all 3 tenants)
- `bun test src/` full unit suite green (no regressions)
- `bunx tsc --noEmit` clean
- Manual (UAT, 05-05): curl /llms.txt on each tenant host → eyeball no cross-tenant facts, ≥200 words once owner prose lands
</verification>

<success_criteria>
llms.txt is per-tenant, leak-free, canonicalUrl-based, and deepened with generated facts + Phase-4 links + EN equivalents; route test green for all tenants; no literal tenant strings remain in route.ts.
</success_criteria>

<output>
Create `.planning/phases/05-llms-txt-depth-measurement/05-02-SUMMARY.md` when done.
</output>
