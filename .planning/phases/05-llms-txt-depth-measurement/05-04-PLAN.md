---
phase: 05-llms-txt-depth-measurement
plan: 04
type: tdd
wave: 3
depends_on: ["05-01", "05-03"]
autonomous: true
requirements: [CONV-01, CONV-02]
files_modified:
  - src/components/FloatingCTA.tsx
  - src/components/FloatingCTAButtons.tsx
  - src/components/FloatingCTAButtons.test.tsx
  - src/components/ContactForm.tsx
  - src/app/[lang]/page.tsx
  - src/app/[lang]/services/[slug]/page.tsx

must_haves:
  truths:
    - "FloatingCTA book link click fires ga4Events.bookOnlineClick (primary key event) and phone link click fires ga4Events.callClick (M-03/M-04, via a thin client island)"
    - "ContactForm success path fires ga4Events.contactFormSubmit (M-03)"
    - "A directions/map link click fires ga4Events.directionsClick (M-03)"
    - "Above the fold on home + service pages: years-experience badge (always) + price-from anchor linking to pricing (always); star rating + review count only when reviewCount > 0 (R-02 gate) (CONV-02)"
    - "FloatingCTA pill remains site-wide and visible above the fold on mobile (CONV-01, verify-only)"
  artifacts:
    - path: "src/components/FloatingCTAButtons.tsx"
      provides: "thin client island wrapping the book + phone anchors with gtag onClick"
      contains: "use client"
    - path: "src/components/FloatingCTAButtons.test.tsx"
      provides: "click-fires-gtag tests for book + phone"
  key_links:
    - from: "src/components/FloatingCTAButtons.tsx"
      to: "src/lib/gtag.ts"
      via: "onClick handlers call ga4Events.bookOnlineClick / callClick"
      pattern: "ga4Events\\.(bookOnlineClick|callClick)"
    - from: "src/components/ContactForm.tsx"
      to: "src/lib/gtag.ts"
      via: "success path calls ga4Events.contactFormSubmit"
      pattern: "ga4Events\\.contactFormSubmit"
    - from: "src/app/[lang]/page.tsx"
      to: "site.reviews / formatFromPrice"
      via: "above-fold trust signals + price-from anchor in hero"
      pattern: "formatFromPrice|reviewCount"
---

<objective>
Vertical slice 3 — conversion. Instrument the four M-03 conversion events on their real click surfaces (FloatingCTA book + phone, ContactForm submit, directions link) and place the three CONV-02 trust/price signals above the fold on home + service pages. The existing FloatingCTA pill is kept (C-01) and instrumented via a thin client island (it is currently an async Server Component and cannot take onClick directly). After this slice, accepting consent and clicking book/call/submit/directions produces GA4 events, and a first-screen visitor sees the experience badge + price-from anchor (+ stars when review data exists).

Purpose: CONV-01/02 turn the measurement plumbing from 05-03 into actual conversion + trust signal capture — the point of the analytics investment.
Output: FloatingCTAButtons island + FloatingCTA refactor + ContactForm/directions instrumentation + above-fold signals.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llms-txt-depth-measurement/05-CONTEXT.md
@.planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md
@.planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md
@.planning/phases/05-01-SUMMARY.md
@.planning/phases/05-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates: FloatingCTAButtons (src/components/FloatingCTAButtons.tsx) + its test. It modifies FloatingCTA.tsx (split: server resolves config, client island renders instrumented anchors), ContactForm.tsx (success-path event), and the home + service detail pages (above-fold signals + directions instrumentation). It consumes ga4Events from src/lib/gtag.ts (05-01). No new npm packages.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FloatingCTAButtons client island + instrument book/phone clicks</name>
  <files>src/components/FloatingCTAButtons.tsx, src/components/FloatingCTAButtons.test.tsx, src/components/FloatingCTA.tsx</files>
  <read_first>
    - src/components/FloatingCTA.tsx (current async Server Component — getStoreConfig, the book anchor to /book-online and the phone anchor to site.contact.phoneHref; markup + classNames to preserve verbatim)
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md, Pattern 4 (FloatingCTA instrumentation: split into server wrapper resolving site + thin FloatingCTAButtons client island; book click -> ga4Events.bookOnlineClick, tel click -> ga4Events.callClick) and Pitfall 3 (async Server Component cannot take onClick)
    - src/lib/gtag.ts (ga4Events emitters from 05-01)
    - .planning/phases/05-llms-txt-depth-measurement/05-VALIDATION.md (CONV-01 book_online_click fires row)
  </read_first>
  <behavior>
    - Test: clicking the book link calls ga4Events.bookOnlineClick (primary key event).
    - Test: clicking the phone link calls ga4Events.callClick.
    - Test: both anchors still render with the same hrefs (/locale/book-online and phoneHref) and labels.
  </behavior>
  <action>
    Create src/components/FloatingCTAButtons.tsx as a use-client island that receives the resolved values as props (bookHref, phoneHref, bookLabel, callLabel, salonLocation) and renders the existing book pill + phone button markup verbatim (copy the JSX + classNames from FloatingCTA), adding onClick handlers: book anchor -> ga4Events.bookOnlineClick(salonLocation), phone anchor -> ga4Events.callClick(phoneHref). Refactor FloatingCTA.tsx to stay the async Server Component that calls getStoreConfig() and renders FloatingCTAButtons passing the resolved props (it keeps owning config resolution; the island owns interactivity — Pitfall 3 split). Preserve the fixed bottom-right positioning wrapper and all classNames so mobile above-fold visibility is unchanged (CONV-01 verify-only). Write FloatingCTAButtons.test.tsx mocking ga4Events (or window.gtag) and asserting the two click events fire with the right names; assert hrefs preserved.
  </action>
  <verify>
    <automated>bun test src/components/FloatingCTAButtons.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - bun test src/components/FloatingCTAButtons.test.tsx green (book + phone events fire; hrefs preserved).
    - FloatingCTAButtons.tsx starts with the use-client directive; FloatingCTA.tsx remains async + calls getStoreConfig and renders FloatingCTAButtons.
    - book event name is book_online_click (M-04 primary key event); phone is call_click.
    - bunx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>FloatingCTA pill preserved; clicks instrumented via thin client island; book/phone events fire; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument ContactForm submit + directions link clicks</name>
  <files>src/components/ContactForm.tsx, src/app/[lang]/services/[slug]/page.tsx</files>
  <read_first>
    - src/components/ContactForm.tsx (already use-client; the try block at the success path setStatus success — add the event right after)
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md, Pattern 4 (ContactForm: ga4Events.contactFormSubmit after setStatus success; Directions links via mapLink in SalonCard / contact page / near-me — add onClick on the directions anchor)
    - src/lib/locations.ts (mapLink generator — where directions anchors are produced)
    - src/lib/gtag.ts (ga4Events)
  </read_first>
  <behavior>
    - Test: on a successful submit, ContactForm calls ga4Events.contactFormSubmit once.
    - Test: a directions/map anchor onClick calls ga4Events.directionsClick.
  </behavior>
  <action>
    In ContactForm.tsx, after setStatus("success") in the success branch, call ga4Events.contactFormSubmit() (M-03). For the directions event, add an onClick calling ga4Events.directionsClick(salonLocation) to the rendered directions/map anchor on a client surface that already renders a mapLink — pick ONE concrete directions anchor that is in a client component (e.g. the directions link in the service detail page area or the SalonCard directions anchor; the executor confirms which mapLink anchor is client-rendered and instruments that one; do not convert a server component solely for this — if the only mapLink anchor is server-rendered, wrap just that anchor in a tiny client link island mirroring FloatingCTAButtons). Add/extend the ContactForm test to assert the success-path event (mock ga4Events). Keep all existing form behavior unchanged.
  </action>
  <verify>
    <automated>bun test src/components/ContactForm.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ContactForm success path calls ga4Events.contactFormSubmit (test green; if ContactForm.test.tsx does not exist, the executor creates it covering the success-path event).
    - At least one directions/map anchor fires ga4Events.directionsClick on click.
    - No regression to the form submit/success/error lifecycle; bunx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>contact_form_submit fires on success; directions_click fires on a real directions anchor; form lifecycle unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Above-fold trust + price-from signals on home + service pages</name>
  <files>src/app/[lang]/page.tsx, src/app/[lang]/services/[slug]/page.tsx</files>
  <read_first>
    - src/app/[lang]/page.tsx (hero section: dict.hero.badges already renders the 15+ experience badge; the reviewCount > 0 + Stars block exists lower in the testimonials area — lift a compact stars+count into the hero, and add a price-from anchor)
    - src/app/[lang]/services/[slug]/page.tsx (formatFromPrice already used for priceDisplay in the What's-included section; AnswerBlock is first-in-main from Phase 3 — add experience badge + price-from anchor + gated stars in the hero/AnswerBlock area)
    - .planning/phases/05-llms-txt-depth-measurement/05-RESEARCH.md, Pattern 8 Above-Fold Trust Signals (per-page insertion table) and Pitfall 7 (R-02 gate: stars only when reviewCount > 0; all tenants currently 0 so stars slot stays empty — expected, not a bug)
    - src/lib/format.ts (formatFromPrice signature) + src/components/Stars.tsx
    - .planning/phases/04-net-new-pages/04-CONTEXT.md (D-29 qualitative-pricing — price-from anchor a partir de $X is compliant; links to /tarifs)
  </read_first>
  <behavior>
    - Test (unit, gate side): the stars block is conditional on site.reviews.reviewCount > 0 (R-02) — covered by existing/extended gate test; UI placement is manual UAT.
    - Above-fold (manual UAT): home + service first screen shows experience badge + price-from anchor; stars appear only when reviewCount > 0.
  </behavior>
  <action>
    On the home page (page.tsx) hero, add a compact above-fold cluster per RESEARCH Pattern 8: (1) experience badge — ensure dict.hero.badges 15+ is present in the first screen (already there; verify, do not duplicate); (2) price-from anchor — render formatFromPrice for the minimum service price as an anchor to /locale/tarifs (a partir de $X / from $X, D-29 compliant); (3) stars+count — a compact Stars + reviewCount rendered ONLY when site.reviews.reviewCount > 0 (R-02 gate, Pitfall 7 — stays empty for current tenants, which is correct). On the service detail page, add the same three above-fold (experience badge + price-from anchor to /tarifs + gated stars) in the hero/AnswerBlock area, reusing the existing priceDisplay/formatFromPrice value. Use existing components (Stars, formatFromPrice) — do not hand-roll (Don't-Hand-Roll table). Comparison + near-me pages get the lighter treatment in 05-05/UAT, not here. No new strings hardcoded — pull price-from label from dict.labels.priceFrom (existing).
  </action>
  <verify>
    <automated>bun test src/</automated>
  </verify>
  <acceptance_criteria>
    - home + service pages render the experience badge and a price-from anchor linking to the tarifs/pricing route above the fold (grep page.tsx for formatFromPrice + a /tarifs or pricing-route link in the hero region).
    - stars block guarded by site.reviews.reviewCount > 0 on both pages (grep for the reviewCount > 0 condition wrapping the Stars usage).
    - bun test src/ full unit suite green; bunx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>Experience badge + price-from anchor above the fold on home + service; stars R-02-gated; existing components reused; no regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser click -> gtag event | conversion event params (phone, salon_location) leave to GA4 |
| config -> above-fold UI | price/review data rendered from getStoreConfig only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | conversion event params to GA4 | accept | only phone number / salon location in params, both already public NAP; consent-gated by 05-03 Consent Mode v2 |
| T-05-04-02 | Tampering | fabricated above-fold review stars | mitigate | stars strictly gated on site.reviews.reviewCount > 0 (R-02); no hardcoded rating — avoids fabricated-review eligibility-cliff risk (REQUIREMENTS out-of-scope: fabricated review schema) |
| T-05-04-SC | Tampering | npm/bun installs | mitigate | no new packages — reuses gtag.ts, Stars, formatFromPrice |
</threat_model>

<verification>
- bun test src/components/FloatingCTAButtons.test.tsx + src/components/ContactForm.test.tsx green
- bun test src/ full unit suite green (no regressions)
- bunx tsc --noEmit clean
- Manual (UAT, 05-05): DevTools mobile emulation — FloatingCTA above fold at scroll 0 + persists; home/service first screen shows badge + price-from; GA4 DebugView shows book/call/form/directions events on consent accept
</verification>

<success_criteria>
Four M-03 conversion events fire on their real surfaces via consent-gated gtag; FloatingCTA pill preserved + instrumented; above-fold experience badge + price-from anchor on home + service with R-02-gated stars; tests green; no new deps.
</success_criteria>

<output>
Create `.planning/phases/05-llms-txt-depth-measurement/05-04-SUMMARY.md` when done.
</output>
