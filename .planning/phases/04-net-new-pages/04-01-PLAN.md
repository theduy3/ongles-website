---
phase: 04-net-new-pages
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/config/schema-invariants.ts
  - src/config/schema-invariants.test.ts
  - src/config/seo/seo.fr.json
  - src/config/seo/seo.en.json
  - src/config/seo/seo-parity.test.ts
  - src/lib/seo.ts
  - src/lib/seo.test.ts
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-03]
must_haves:
  truths:
    - "checkWordCount, checkCrossTenantOverlap, checkRoutePresence exist and are exported from schema-invariants.ts"
    - "measureSentenceOverlap returns 1.0 for identical text and 0 for disjoint text"
    - "pricingGraph(lang, items, cfg) returns @type ItemList with per-item AggregateOffer lowPrice/highPrice when priceTo > price"
    - "Base seo.{fr,en}.json define the pages.pricing / pages.comparison.* / pages.nearMe key structure (type source)"
    - "seo-parity.test.ts asserts the new pages.* keys are present and type-string in both locales for base + 3 tenants"
  prohibitions:
    - "schema-invariants.ts MUST NOT import from src/lib/seo.ts or use any @/ alias (Docker SWC require-hook breaks)"
    - "new guards MUST NOT be called from validateSchemaInvariants() in this plan (gate stays green until content lands in 04-05)"
    - "no new external package; no content.{locale}.json writes"
    - "Phase 2 builders offer()/servicesGraph()/serviceGraph() MUST NOT be modified (D-28)"
  artifacts:
    - path: "src/config/schema-invariants.ts"
      provides: "checkWordCount, checkCrossTenantOverlap, measureSentenceOverlap, checkRoutePresence (exported, UNWIRED)"
    - path: "src/lib/seo.ts"
      provides: "pricingGraph thin wrapper over servicesGraph"
    - path: "src/config/seo/seo.fr.json + seo.en.json"
      provides: "pages.* key scaffold (type source)"
  key_links:
    - from: "src/config/schema-invariants.test.ts"
      to: "src/config/schema-invariants.ts"
      via: "imports checkWordCount/measureSentenceOverlap/checkCrossTenantOverlap and asserts fail-fixture behaviour"
      pattern: "measureSentenceOverlap|checkWordCount|checkCrossTenantOverlap"
---

<objective>
Build the RED-first guard machinery, JSON key scaffold, parity coverage, and pricing ItemList wrapper that every Phase 4 page slice depends on — without flipping the build gate. This is the Wave 0 dependency for plans 04-02/03/04/05.

Purpose: TDD foundation. Mirrors Phase 3's 03-01 (write unwired guards + stub JSON + parity RED) so per-page slices in later waves only author content and wire pages, then 04-05 activates the gate. Covers the testable cores of PAGE-01 (ItemList shape), PAGE-02/PAGE-03 (word-count + overlap algorithms).
Output: new exported (but unwired) guard functions, the `pages.*` seo key namespace in base + tenant files, extended parity test, `pricingGraph()` wrapper, and a passing ItemList shape test — all green, build still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-net-new-pages/04-CONTEXT.md
@.planning/phases/04-net-new-pages/04-RESEARCH.md
@.planning/phases/04-net-new-pages/04-VALIDATION.md
@.planning/phases/03-content-depth/03-05-SUMMARY.md
</context>

## Artifacts this phase produces (04-01)

| Artifact | Kind | Notes |
|----------|------|-------|
| `checkWordCount()` | NEW export in schema-invariants.ts | comparison body ≥200 / nearMe answerBlock ≥150; UNWIRED |
| `measureSentenceOverlap()` | NEW export | normalized-sentence Jaccard, pure, alias-free |
| `checkCrossTenantOverlap()` | NEW export | iterates TENANT_REGISTRY pairs on nearMe answerBlock per locale; UNWIRED |
| `checkRoutePresence()` | NEW export | asserts new localized route slugs registered per tenant; UNWIRED |
| `NEW_PAGE_OVERLAP_THRESHOLD = 0.30`, `COMPARISON_WORD_FLOOR = 200`, `NEAR_ME_WORD_FLOOR = 150` constants | NEW | LOCKED thresholds |
| `pages.pricing.*`, `pages.comparison.{4 slugs}.*`, `pages.nearMe.*` | NEW seo JSON keys | base + 3 tenant files (scaffold) |
| `pricingGraph()` | NEW export in src/lib/seo.ts | thin wrapper calling servicesGraph |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — guard unit tests + parity extension + ItemList shape test (failing)</name>
  <files>src/config/schema-invariants.test.ts, src/config/seo/seo-parity.test.ts, src/lib/seo.test.ts</files>
  <read_first>
    - src/config/schema-invariants.ts (existing splitSentences/countWords/isAnswerBlockInsufficient + checkFaqFloor extension pattern)
    - src/config/schema-invariants.test.ts (existing test layout to mirror)
    - src/config/seo/seo-parity.test.ts (keyPaths + pairs array + seoTenantPairs extension point)
    - src/lib/seo.ts lines 310-343 (ServiceItem type + servicesGraph output shape)
    - .planning/phases/03-content-depth/03-05-SUMMARY.md (fail-fixture proof precedent)
  </read_first>
  <behavior>
    - measureSentenceOverlap: identical 3-sentence text → 1.0; fully disjoint → 0; ~25% shared sentences → < 0.30; ~35% shared → >= 0.30
    - checkWordCount over a fixture seo source with a 10-word comparison body → returns an error citing the ≥200 floor; a ≥200-word body → no error. nearMe answerBlock under 150 words → error.
    - checkCrossTenantOverlap over two fixtures with identical nearMe answerBlock → returns an error citing ≥0.30; distinct → no error
    - checkRoutePresence over a tenant config missing the pricing localized route → error; present → no error
    - pricingGraph(lang, items, cfg) output: @type === "ItemList"; itemListElement[i].offers["@type"] === "AggregateOffer" with lowPrice/highPrice for an item where priceTo > price
    - seo-parity: new pages.pricing.answerBlock / pages.comparison.gel-vs-acrylique.body / pages.nearMe.answerBlock keys typeof string in both fr+en for base + 3 tenants
  </behavior>
  <action>
    Author failing tests FIRST (no implementation yet). In schema-invariants.test.ts add a describe block "Phase 4 net-new-page guards" importing the not-yet-existing exports measureSentenceOverlap, checkWordCount, checkCrossTenantOverlap, checkRoutePresence and the constants COMPARISON_WORD_FLOOR, NEAR_ME_WORD_FLOOR, NEW_PAGE_OVERLAP_THRESHOLD. Build small inline fixtures (plain objects shaped like SeoAnswerSource extended with a pages field) — do NOT depend on real tenant JSON for the algorithm tests so they stay deterministic. For checkCrossTenantOverlap/checkRoutePresence which read module-level TENANT data, assert their type signature and the pure helper measureSentenceOverlap directly; cross-tenant integration is proven in 04-05. In seo.test.ts add a describe "pricingGraph" importing pricingGraph from @/lib/seo, call it with two ServiceItem fixtures (one with priceTo>price, one without) and assert the ItemList + AggregateOffer/Offer shape. In seo-parity.test.ts extend the existing seoTenantPairs typeof-string assertion block to cover doc.pages.pricing.answerBlock, doc.pages.pricing.answerHeading, doc.pages.comparison["gel-vs-acrylique"].body, doc.pages.nearMe.answerBlock, doc.pages.nearMe.answerHeading for fr+en; widen the SeoDoc type to include the pages shape. Run the suite; confirm these new tests FAIL (missing exports / missing keys) while all 282 existing tests still pass. Commit RED.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts src/lib/seo.test.ts src/config/seo/seo-parity.test.ts 2>&1 | grep -E "fail|error" | head</automated>
  </verify>
  <done>New Phase-4 tests are present and RED (fail on missing exports/keys); the rest of the suite is unaffected; RED commit made (test(04-01): ...).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement unwired guards, pricingGraph wrapper, and JSON key scaffold</name>
  <files>src/config/schema-invariants.ts, src/lib/seo.ts, src/config/seo/seo.fr.json, src/config/seo/seo.en.json</files>
  <read_first>
    - src/config/schema-invariants.ts (header alias-free constraint lines 1-14; splitSentences line 115; countWords line 136; TENANT_SEO line 183; CONTENT_LOCALES line 149; EXCLUDED_TENANTS line 59)
    - src/lib/seo.ts lines 310-343 (servicesGraph + ServiceItem)
    - src/config/seo/seo.fr.json + seo.en.json (base type-source shape: meta/services/locations top-level keys)
    - src/config/tenants/ongles-maily/seo.fr.json (tenant shape to mirror)
  </read_first>
  <action>
    In schema-invariants.ts add the LOCKED constants COMPARISON_WORD_FLOOR = 200, NEAR_ME_WORD_FLOOR = 150, NEW_PAGE_OVERLAP_THRESHOLD = 0.30 (as const). Add NEW_COMPARISON_SLUGS = ["pose-vs-remplissage","manucure-vs-pedicure","gel-vs-acrylique","meilleur-pour"] (FR-key namespace used in seo JSON). Extend the SeoAnswerSource type with an optional `pages` field (pricing/comparison/nearMe per the 04-RESEARCH Q5 shape). Implement and EXPORT: measureSentenceOverlap(textA, textB) using the normalized-sentence Jaccard from 04-RESEARCH Q3 (lowercase, strip non-word punctuation, collapse whitespace, splitSentences, Set Jaccard); checkWordCount() reading pages.comparison.*.body (floor 200) and pages.nearMe.answerBlock (floor 150) from TENANT_SEO per CONTENT_LOCALES using countWords; checkCrossTenantOverlap() iterating distinct tenant pairs in TENANT_SEO per locale, measuring overlap on pages.nearMe.answerBlock, erroring when >= NEW_PAGE_OVERLAP_THRESHOLD; checkRoutePresence() asserting each tenant's site.routes (from TENANT_REGISTRY) contains the new locale-agnostic route markers it owns (planner note: near-me + pricing presence — exact registration scheme finalized in 04-05; for now check the FR pricing slug constant is present-or-absent and return a structured error list). Keep ALL imports relative/alias-free. Do NOT call any new function from validateSchemaInvariants() yet. In src/lib/seo.ts add `export function pricingGraph(lang, items, cfg)` that returns `servicesGraph(lang, items, cfg)` unchanged (named entry point per 04-RESEARCH primary recommendation; Phase 2 builders untouched). In base seo.fr.json AND seo.en.json add the `pages` object with pricing{answerHeading,answerBlock,metaTitle,metaDescription}, comparison{4 slugs each: answerHeading,answerBlock,body}, nearMe{answerHeading,answerBlock,boroughName,body,metaTitle,metaDescription} — placeholder strings that satisfy typeof-string (real copy lands per-tenant in later waves). Keys identical fr/en. Run tests to GREEN. Commit.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts src/lib/seo.test.ts src/config/seo/seo-parity.test.ts</automated>
  </verify>
  <done>All Phase-4 foundation tests pass; full `bun test src/` is green; `bun run build` still succeeds (new guards unwired); pricingGraph exported; pages.* scaffold present and parity-clean in base + tenant files. GREEN commit made.</done>
</task>

</tasks>

<verification>
- `bun test src/` → all green (existing 282 + new Phase-4 tests)
- `bun run build` → still succeeds (no new guard wired into validateSchemaInvariants yet)
- `grep -n "export function pricingGraph" src/lib/seo.ts` → present
- `grep -nE "export function (checkWordCount|checkCrossTenantOverlap|checkRoutePresence)|export function measureSentenceOverlap" src/config/schema-invariants.ts` → all present
- offer()/servicesGraph()/serviceGraph() bodies unchanged (D-28) — git diff shows additions only
</verification>

<success_criteria>
Guard functions + overlap algorithm + pricingGraph + pages.* key namespace exist, are unit-tested, parity-clean, and unwired. Later page slices author into pages.* and wire routes; 04-05 flips the gate.
</success_criteria>

<output>
Create `.planning/phases/04-net-new-pages/04-01-SUMMARY.md` when done.
</output>
