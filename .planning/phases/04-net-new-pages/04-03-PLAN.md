---
phase: 04-net-new-pages
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/[lang]/beauport/page.tsx
  - src/app/[lang]/charlesbourg/page.tsx
  - src/app/[lang]/trois-rivieres/page.tsx
  - src/components/NearMeDetails.tsx
  - src/config/tenants/ongles-maily/seo.fr.json
  - src/config/tenants/ongles-maily/seo.en.json
  - src/config/tenants/ongles-charlesbourg/seo.fr.json
  - src/config/tenants/ongles-charlesbourg/seo.en.json
  - src/config/tenants/ongles-rivieres/seo.fr.json
  - src/config/tenants/ongles-rivieres/seo.en.json
autonomous: false
requirements: [PAGE-03]
user_setup: []
must_haves:
  truths:
    - "Each tenant has one borough near-me route (maily→/beauport, charlesbourg→/charlesbourg, rivieres→/trois-rivieres) returning 200 on FR and EN"
    - "Near-me page leads with <AnswerBlock>; the ≥150-word unique opener doubles as the answer block, h1 names the borough not the shared city (P-10)"
    - "pages.nearMe.answerBlock is ≥150 words per tenant per locale (checkWordCount goes live this slice)"
    - "Cross-tenant sentence overlap on pages.nearMe.answerBlock is <30% for every tenant pair per locale (checkCrossTenantOverlap goes live this slice)"
    - "Local details panel shows NAP + landmark + hours + service links from getStoreConfig()"
  prohibitions:
    - "near-me MUST NOT appear in header nav / site.nav (SEO landing only, P-04)"
    - "borough copy MUST NOT reuse the shared-city framing across tenants (the two Québec tenants must differentiate on Beauport vs Charlesbourg)"
    - "no generateStaticParams / per-file dynamic; no hardcoded tenant data"
    - "no exact prices in the near-me prose (qualitative only, D-29)"
  artifacts:
    - path: "src/app/[lang]/beauport/page.tsx (+ charlesbourg, trois-rivieres)"
      provides: "borough near-me routes, on-demand"
      exports: ["default", "generateMetadata"]
    - path: "src/components/NearMeDetails.tsx"
      provides: "local NAP/landmark/hours/service-link panel"
  key_links:
    - from: "src/config/schema-invariants.ts"
      to: "src/config/tenants/*/seo.*.json"
      via: "checkWordCount + checkCrossTenantOverlap now CALLED from validateSchemaInvariants over pages.nearMe"
      pattern: "checkCrossTenantOverlap|checkWordCount"
    - from: "src/app/[lang]/beauport/page.tsx"
      to: "src/lib/store-config.ts"
      via: "getStoreConfig() NAP for local details panel"
      pattern: "getStoreConfig"
---

<objective>
Vertical slice for PAGE-03: one borough near-me landing per tenant, reachable FR+EN, leading with a hand-authored ≥150-word answer block that is unique across tenants (<30% overlap, build-guard enforced). This is the slice with the real cross-tenant duplication risk (two Québec tenants), so the overlap guard activates here.

Purpose: borough-targeted SEO/GEO landing; differentiates the two Québec-City tenants on Beauport vs Charlesbourg to avoid cannibalization.
Output: 3 borough routes, NearMeDetails panel, authored unique openers, and the now-LIVE word-count + cross-tenant-overlap guards over near-me copy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-net-new-pages/04-CONTEXT.md
@.planning/phases/04-net-new-pages/04-RESEARCH.md
@.planning/phases/04-net-new-pages/04-UI-SPEC.md
@.planning/phases/04-net-new-pages/04-01-SUMMARY.md
@.planning/phases/04-net-new-pages/04-02-SUMMARY.md
</context>

## Artifacts this phase produces (04-03)

| Artifact | Kind | Notes |
|----------|------|-------|
| `src/app/[lang]/{beauport,charlesbourg,trois-rivieres}/page.tsx` | NEW routes | one per tenant; on-demand |
| `src/components/NearMeDetails.tsx` | NEW component | NAP + landmark + hours + service links |
| `pages.nearMe.*` per-tenant copy | authored JSON | ≥150-word unique opener, borough-targeted |
| `checkWordCount()` + `checkCrossTenantOverlap()` wired | guard activation | called from validateSchemaInvariants (near-me scope) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author borough near-me copy + activate word-count & overlap guards over near-me</name>
  <files>src/config/tenants/{ongles-maily,ongles-charlesbourg,ongles-rivieres}/seo.fr.json, .../seo.en.json, src/config/schema-invariants.ts, src/config/schema-invariants.test.ts</files>
  <read_first>
    - src/config/schema-invariants.ts (checkWordCount/checkCrossTenantOverlap/measureSentenceOverlap from 04-01; validateSchemaInvariants lines 513-541 wiring point; CONTENT_LOCALES; EXCLUDED_TENANTS)
    - src/config/schema-invariants.test.ts (Phase-4 guard tests from 04-01)
    - src/config/tenants/ongles-maily/seo.fr.json (pages.nearMe scaffold; NAP/landmark facts)
    - src/config/tenants/ongles-charlesbourg/site.ts + ongles-rivieres/site.ts (borough facts, landmarks, addresses)
    - .planning/phases/04-net-new-pages/04-UI-SPEC.md §C (near-me layout contract)
    - .planning/phases/03-content-depth/03-05-SUMMARY.md (gate-activation + fail-fixture precedent)
  </read_first>
  <behavior>
    - After authoring, validateSchemaInvariants() returns zero errors for near-me word-count and overlap
    - A fail-fixture test proves checkCrossTenantOverlap fires when two tenants' nearMe answerBlock are made identical, and checkWordCount fires when a nearMe answerBlock is <150 words
  </behavior>
  <action>
    Hand-author pages.nearMe per tenant in seo.fr.json + seo.en.json (replace 04-01 placeholders). maily targets BEAUPORT, charlesbourg targets CHARLESBOURG, rivieres targets TROIS-RIVIÈRES — answerHeading names the borough/landmark (not the shared "Québec" city) per P-10. answerBlock is the ≥150-word unique opener that doubles as the answer block (P-12): first sentence stands alone if quoted (D-22), borough-specific landmarks/streets/access details so the two Québec tenants share <30% sentences (vous D-21, idiomatic EN D-24, qualitative pricing only D-29). Set boroughName, body (extra local detail), metaTitle, metaDescription. Keep keys identical fr/en. Then WIRE the guards: in validateSchemaInvariants() add `errors.push(...checkWordCount()); errors.push(...checkCrossTenantOverlap());` (near-me scope active now; comparison word-count begins covering once 04-04 authors comparison bodies — checkWordCount over empty/placeholder comparison.body must be tolerant until then, so scope checkWordCount's comparison branch to skip entries whose body is the known placeholder OR gate comparison coverage behind a slug-present check — finalize comparison activation in 04-05). Extend schema-invariants.test.ts with a fail-fixture proving overlap fires at identical text and word-count fires below 150 (mirror 03-05). Run tests + build.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts && bun run build 2>&1 | tail -15</automated>
  </verify>
  <done>Near-me copy authored + parity-clean; checkWordCount + checkCrossTenantOverlap wired and green over near-me; fail-fixture proves both bite; `bun run build` succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NearMeDetails panel + 3 borough route pages</name>
  <files>src/components/NearMeDetails.tsx, src/app/[lang]/{beauport,charlesbourg,trois-rivieres}/page.tsx</files>
  <read_first>
    - src/app/[lang]/services/[slug]/page.tsx (on-demand pattern, isLocale guard, JsonLd, AnswerBlock first-in-main)
    - src/app/[lang]/tarifs/page.tsx (from 04-02 — sibling on-demand named-folder page to mirror)
    - src/lib/seo.ts (pageMetadata routeByLocale, breadcrumbGraph)
    - src/components/AnswerBlock.tsx
    - .planning/phases/04-net-new-pages/04-UI-SPEC.md §C
  </read_first>
  <behavior>
    - /fr/beauport (maily) → 200; /en/beauport → 200 EN; wrong-locale stays 200 only for its borough's tenant
    - panel shows address, landmark, hours, and links to service detail pages
  </behavior>
  <action>
    Create NearMeDetails.tsx (server component) rendering a white shadow-card panel with address + landmark (getStoreConfig NAP), hours, and a compact service list linking to service detail paths (P-19) — reuse existing NAP/hours presentation tokens; no map embed. Create the three borough page.tsx files following the 04-02 named-folder pattern EXACTLY: no generateStaticParams / per-file dynamic; `if (!isLocale(lang)) notFound()` first. Each is tenant-targeted but slug is universal (P-02 one Docker image): the borough folder renders the active tenant's nearMe copy — author each page to read pages.nearMe from getSeo(lang) so any tenant deploys the same route, but copy differs per tenant (the borough folder name matches the intended tenant's borough; cross-tenant access still renders that tenant's own nearMe). generateMetadata uses pageMetadata with routeByLocale matching the borough's fr/en slug (FR/EN borough slugs identical here unless localized — use same slug both locales, planner note: boroughs are proper nouns; if EN differs author distinct routeByLocale). Render breadcrumbGraph JsonLd (Home → Locations → Borough), `<AnswerBlock heading={seo.pages.nearMe.answerHeading} text={seo.pages.nearMe.answerBlock} />` first-in-main (carries h1), then `<NearMeDetails .../>`, then Button → booking + ghost call + cross-link to /{lang}/locations and home (P-19). Add the near-me routes to each tenant's site.routes for sitemap inclusion (NOT site.nav — P-04). Run build.
  </action>
  <verify>
    <automated>bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>NearMeDetails + 3 borough pages compile; build green; AnswerBlock-led with single h1; near-me added to site.routes (not site.nav); cross-links present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — borough copy unique, borough-targeted, not in header</name>
  <action>Human confirms borough-targeted copy, uniqueness across tenants, and nav exclusion before this slice closes. No code change — verification only.</action>
  <what-built>Borough near-me landings (Beauport / Charlesbourg / Trois-Rivières) with ≥150-word unique answer-first opener, NAP/landmark/hours panel, cross-links — guard-enforced <30% cross-tenant overlap.</what-built>
  <how-to-verify>
    1. `bun run dev` with `TENANT=ongles-maily`, visit http://localhost:3000/fr/beauport — confirm h1 names Beauport (not generic Québec), the opening paragraph reads as a unique ≥150-word local description, NAP/landmark/hours panel below, book + call CTAs.
    2. Visit /en/beauport — idiomatic EN, same facts.
    3. Switch `TENANT=ongles-charlesbourg`, visit /fr/charlesbourg — confirm the copy is clearly DIFFERENT from Beauport (different borough landmarks/streets), not a find-replace of the city name.
    4. Confirm the near-me page is NOT in the header nav (it is a landing, reachable via footer/locations/sitemap only).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (city-not-borough h1, copy too similar across tenants, near-me leaked into header).</resume-signal>
</task>

</tasks>

<verification>
- `bun test src/` green (incl. overlap + word-count fail-fixtures)
- `bun run build` green with checkWordCount + checkCrossTenantOverlap LIVE over near-me
- `curl -s localhost:3000/fr/beauport -o /dev/null -w "%{http_code}"` → 200; /en/beauport → 200
- `grep -L beauport src/config/tenants/ongles-maily/site.ts` (near-me in routes not nav) — manual confirm site.nav unchanged
- seo-parity test green for pages.nearMe keys
</verification>

<success_criteria>
PAGE-03 success criterion 3 met: near-me pages exist per tenant with ≥150-word unique opening copy and <30% cross-tenant sentence overlap, build-guard enforced.
</success_criteria>

<output>
Create `.planning/phases/04-net-new-pages/04-03-SUMMARY.md` when done.
</output>
