---
phase: 04-net-new-pages
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/[lang]/tarifs/page.tsx
  - src/app/[lang]/pricing/page.tsx
  - src/components/PricingTable.tsx
  - src/config/tenants/ongles-maily/seo.fr.json
  - src/config/tenants/ongles-maily/seo.en.json
  - src/config/tenants/ongles-charlesbourg/seo.fr.json
  - src/config/tenants/ongles-charlesbourg/seo.en.json
  - src/config/tenants/ongles-rivieres/seo.fr.json
  - src/config/tenants/ongles-rivieres/seo.en.json
  - src/dictionaries/fr.json
  - src/dictionaries/en.json
autonomous: false
requirements: [PAGE-01]
user_setup: []
must_haves:
  truths:
    - "GET /fr/tarifs returns 200 and renders a service price list from config (pose-ongles 60-75, remplissage 45-60, soins-mains 30-40, soins-pieds 35-60)"
    - "GET /en/pricing returns 200 with the EN copy; GET /en/tarifs and /fr/pricing return 404 (wrong-locale guard)"
    - "Pricing page emits ItemList + per-row AggregateOffer JSON-LD via pricingGraph() rendered through <JsonLd>"
    - "Page leads with <AnswerBlock> carrying the single <h1> ('Combien coûtent nos services ?' / 'What do our services cost?')"
    - "pages.pricing copy authored per tenant in seo.{fr,en}.json with FR/EN parity"
  prohibitions:
    - "no export const dynamic and no generateStaticParams in the new page files (inherited from [lang] layout)"
    - "no duration column, no per-row blurb (P-14 price-only)"
    - "no new JSON-LD builder; reuse pricingGraph/servicesGraph (D-28 Phase 2 builders untouched)"
    - "no hardcoded tenant data; resolve via getStoreConfig()/getSeo()"
  artifacts:
    - path: "src/app/[lang]/tarifs/page.tsx"
      provides: "FR pricing route (render on demand)"
      exports: ["default", "generateMetadata"]
    - path: "src/app/[lang]/pricing/page.tsx"
      provides: "EN pricing route"
      exports: ["default", "generateMetadata"]
    - path: "src/components/PricingTable.tsx"
      provides: "responsive price list (no h-scroll on mobile)"
  key_links:
    - from: "src/app/[lang]/tarifs/page.tsx"
      to: "src/lib/seo.ts"
      via: "pricingGraph(lang, items, cfg) → <JsonLd>"
      pattern: "pricingGraph"
    - from: "src/app/[lang]/tarifs/page.tsx"
      to: "src/lib/seo.ts"
      via: "pageMetadata with routeByLocale { fr:'/tarifs', en:'/pricing' }"
      pattern: "routeByLocale"
---

<objective>
Vertical slice for PAGE-01: a reachable, schema-valid, answer-first pricing page per tenant in both locales. After this plan a user can visit /fr/tarifs (or /en/pricing) and see config-sourced prices + book CTA, and AI/Google can extract ItemList+AggregateOffer.

Purpose: delivers the highest-intent net-new route end-to-end (route + copy + schema + UI + nav membership groundwork) reusing the Phase 2 builder and Phase 3 AnswerBlock.
Output: FR + EN pricing routes, PricingTable component, per-tenant pricing copy, nav dictionary keys.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-net-new-pages/04-CONTEXT.md
@.planning/phases/04-net-new-pages/04-RESEARCH.md
@.planning/phases/04-net-new-pages/04-UI-SPEC.md
@.planning/phases/04-net-new-pages/04-01-SUMMARY.md
</context>

## Artifacts this phase produces (04-02)

| Artifact | Kind | Notes |
|----------|------|-------|
| `src/app/[lang]/tarifs/page.tsx` | NEW FR route | on-demand, isLocale guard |
| `src/app/[lang]/pricing/page.tsx` | NEW EN route | on-demand, isLocale guard |
| `src/components/PricingTable.tsx` | NEW component | responsive, gold price emphasis |
| `pages.pricing.*` per-tenant copy | authored JSON | replaces 04-01 placeholders in 3 tenants |
| `dict.nav.pricing` | NEW dict key (fr+en) | label for header nav (membership wired in 04-05) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PricingTable component + per-tenant pricing copy</name>
  <files>src/components/PricingTable.tsx, src/config/tenants/{ongles-maily,ongles-charlesbourg,ongles-rivieres}/seo.fr.json, .../seo.en.json, src/dictionaries/fr.json, src/dictionaries/en.json</files>
  <read_first>
    - src/components/AnswerBlock.tsx (token + section conventions to match)
    - src/app/[lang]/services/[slug]/page.tsx lines 158-176 (existing price dl markup + tokens)
    - .planning/phases/04-net-new-pages/04-UI-SPEC.md §A (Pricing page layout contract)
    - src/config/tenants/ongles-maily/seo.fr.json (pages.* scaffold from 04-01; service names/prices)
    - src/config/types.ts (Service: price/priceTo/slug{fr,en})
    - src/dictionaries/en.json nav block
  </read_first>
  <behavior>
    - PricingTable renders one row per passed service: name (links to that service detail path) + price range "60 $ – 75 $" style, gold-emphasized; stacks label-over-value below sm with no horizontal scroll
    - empty/missing priceTo falls back to single price display
  </behavior>
  <action>
    Create PricingTable.tsx as a server component taking `services` (id, name, href, price, priceTo) + a `labels`/formatting helper and `lang`. Render a semantic list (a `<dl>` or `<table>`) inside a single white shadow-card panel `max-w-3xl mx-auto`, rows separated by `divide-y divide-sand`, service name as a Link `text-espresso hover:text-gold` to the service detail path (cross-link P-19), price range right-aligned in `text-gold font-medium`. Mobile: rows stack label-over-value, full width, no horizontal scroll (P-13). Price-only — no duration, no blurb (P-14). Use config values only. Author pages.pricing copy in all 3 tenants' seo.fr.json + seo.en.json (replace 04-01 placeholders): answerHeading FR "Combien coûtent nos services ?" / EN "What do our services cost?", answerBlock ≥2 self-contained sentences answering "what do services cost" leading with the tenant's price range framing (vous, D-21; idiomatic EN, D-24; per-tenant facts D-23), plus metaTitle/metaDescription. Keep keys identical fr/en. Add dict.nav.pricing to dictionaries/fr.json ("Tarifs") and en.json ("Pricing"). Run seo-parity + F-02 dictionary parity tests.
  </action>
  <verify>
    <automated>bun test src/config/seo/seo-parity.test.ts</automated>
  </verify>
  <done>PricingTable component exists; pages.pricing copy authored + parity-clean across 3 tenants fr/en; dict.nav.pricing present both locales; parity tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FR /tarifs + EN /pricing routes with ItemList JSON-LD and AnswerBlock</name>
  <files>src/app/[lang]/tarifs/page.tsx, src/app/[lang]/pricing/page.tsx</files>
  <read_first>
    - src/app/[lang]/services/[slug]/page.tsx (EXACT on-demand pattern: isLocale guard, generateMetadata, getStoreConfig/getSeo/getDictionary, JsonLd usage, AnswerBlock first-in-main)
    - src/lib/seo.ts lines 101-141 (pageMetadata + routeByLocale), 322-343 (servicesGraph/pricingGraph), 402-417 (breadcrumbGraph)
    - src/lib/services.ts (servicePath, servicePathsByLocale)
    - src/components/PricingTable.tsx (from Task 1)
    - .planning/phases/04-net-new-pages/04-UI-SPEC.md §A
  </read_first>
  <behavior>
    - /fr/tarifs → 200, AnswerBlock h1 first in main, PricingTable, book CTA; ItemList+AggregateOffer in page head
    - /en/pricing → 200 EN copy; /en/tarifs and /fr/pricing → 404
  </behavior>
  <action>
    Create tarifs/page.tsx (FR) and pricing/page.tsx (EN) following the service-detail template EXACTLY: no generateStaticParams, no per-file `export const dynamic`. First line of both generateMetadata and default export guards `if (!isLocale(lang)) return {} / notFound()`. Additionally restrict each folder to its own locale: tarifs returns notFound() for non-fr, pricing returns notFound() for non-en (Pitfall 1 — wrong-locale guard). generateMetadata calls pageMetadata(lang, "/tarifs" or "/pricing", { title: seo.pages.pricing.metaTitle, description: seo.pages.pricing.metaDescription, routeByLocale: { fr: "/tarifs", en: "/pricing" } }, { site, locations }). Default export: resolve getStoreConfig()/getSeo(lang)/getDictionary(lang); build the items array from services (name from dict.serviceDetails[id].title, description from seo.services[id].schemaDescription, price/priceTo, path via servicePath); render `<JsonLd data={pricingGraph(lang, items, { site, locations })} />` and a breadcrumbGraph JsonLd (Home → Pricing). Render `<AnswerBlock heading={seo.pages.pricing.answerHeading} text={seo.pages.pricing.answerBlock} link={{ href: \`/${lang}/services\`, label: dict.cta.viewServices }} />` as first-in-main, then `<PricingTable .../>`, then a centered Button → `/${lang}${site.booking}` (dict.cta.book) + ghost call link to site.contact.phoneHref (P-19 cross-links + CTA). Both files share structure; EN differs only by slug + locale guard. Run build to confirm compile + the pricing answerBlock passes the existing D-11 ≥2-sentence presence shape (it is exercised live in 04-05).
  </action>
  <verify>
    <automated>bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>Both route files compile; `bun run build` green; ItemList JSON-LD wired via pricingGraph; AnswerBlock carries the single h1; no generateStaticParams / per-file dynamic; wrong-locale folders notFound().</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — pricing pages render, schema valid, locale-correct</name>
  <action>Human confirms rendered pricing pages, JSON-LD, prices, and locale guards before this slice closes. No code change — verification only.</action>
  <what-built>FR /tarifs + EN /pricing pages: AnswerBlock-led, config-sourced price list, ItemList+AggregateOffer JSON-LD, book CTA, service cross-links.</what-built>
  <how-to-verify>
    1. Run `bun run dev` with `TENANT=ongles-maily`. Visit http://localhost:3000/fr/tarifs — confirm the h1 answer heading appears first, the four services show with $60–75 / $45–60 / $30–40 / $35–60 ranges, prices in gold, and a Book CTA. No horizontal scroll on a narrow viewport.
    2. Visit http://localhost:3000/en/pricing — confirm idiomatic EN copy and the same prices.
    3. Visit http://localhost:3000/en/tarifs and http://localhost:3000/fr/pricing — confirm both 404.
    4. View source on /fr/tarifs: confirm a `<script type="application/ld+json">` containing `"@type":"ItemList"` with `"@type":"AggregateOffer"` lowPrice/highPrice. Optionally paste into Google Rich Results Test (PAGE-01 manual gate).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (wrong price, missing schema, locale leak, mobile scroll).</resume-signal>
</task>

</tasks>

<verification>
- `bun test src/` green; `bun run build` green
- `curl -s localhost:3000/fr/tarifs -o /dev/null -w "%{http_code}"` → 200; `/en/pricing` → 200; `/en/tarifs` → 404
- view-source grep `"@type":"ItemList"` and `"@type":"AggregateOffer"` present on pricing page
- seo-parity test green (pages.pricing keys fr/en parity across tenants)
</verification>

<success_criteria>
PAGE-01 success criterion 1 met for all tenants: pricing page reachable FR+EN, renders config pricing, emits valid ItemList+AggregateOffer, answer-first.
</success_criteria>

<output>
Create `.planning/phases/04-net-new-pages/04-02-SUMMARY.md` when done.
</output>
