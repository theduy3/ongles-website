---
phase: 04-net-new-pages
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/app/[lang]/comparaisons/[slug]/page.tsx
  - src/app/[lang]/comparisons/[slug]/page.tsx
  - src/components/ComparisonColumns.tsx
  - src/lib/comparisons.ts
  - src/config/tenants/ongles-maily/seo.fr.json
  - src/config/tenants/ongles-maily/seo.en.json
  - src/config/tenants/ongles-charlesbourg/seo.fr.json
  - src/config/tenants/ongles-charlesbourg/seo.en.json
  - src/config/tenants/ongles-rivieres/seo.fr.json
  - src/config/tenants/ongles-rivieres/seo.en.json
  - src/dictionaries/fr.json
  - src/dictionaries/en.json
  - src/config/schema-invariants.ts
autonomous: false
requirements: [PAGE-02]
user_setup: []
must_haves:
  truths:
    - "4 comparison pages exist (pose-vs-remplissage, manucure-vs-pedicure, gel-vs-acrylique, meilleur-pour) reachable FR + EN, wrong-locale 404"
    - "Each comparison page leads with <AnswerBlock> (verdict in first sentence) and has ≥200 words of unique body copy per locale"
    - "Pages render the fixed template: AnswerBlock → two-column comparison cards → 'which to choose' → CTA (P-09)"
    - "checkWordCount comparison ≥200 branch is fully LIVE over all 4 comparison bodies; answerBlock ≥2-sentence presence covers comparison routes"
    - "gel-vs-acrylique reflects real services (both gel and acrylic offered, P-06)"
  prohibitions:
    - "comparison MUST NOT use a dense data table — two-column CARDS per UI-SPEC §B locked decision"
    - "no generateStaticParams / per-file dynamic; no hardcoded tenant data"
    - "no exact prices in comparison prose (qualitative, D-29)"
    - "no content.{locale}.json writes"
  artifacts:
    - path: "src/lib/comparisons.ts"
      provides: "comparison slug registry + FR/EN slug resolver (mirrors services.ts pattern)"
    - path: "src/app/[lang]/comparaisons/[slug]/page.tsx + comparisons/[slug]/page.tsx"
      provides: "FR + EN comparison routes via shared template"
    - path: "src/components/ComparisonColumns.tsx"
      provides: "two-column comparison cards"
  key_links:
    - from: "src/app/[lang]/comparaisons/[slug]/page.tsx"
      to: "src/lib/comparisons.ts"
      via: "comparisonBySlug(lang, slug) resolves FR slug → comparison record or notFound"
      pattern: "comparisonBySlug"
    - from: "src/config/schema-invariants.ts"
      to: "src/config/tenants/*/seo.*.json"
      via: "checkWordCount comparison ≥200 branch + answerBlock presence over pages.comparison.*"
      pattern: "COMPARISON_WORD_FLOOR"
---

<objective>
Vertical slice for PAGE-02: the 4 comparison/decision pages on a shared template, reachable FR+EN, each answer-first with ≥200 words of unique copy. After this plan AI engines and high-intent searchers have buying-guide pages for the real service pairs.

Purpose: covers decision/comparison search + AI-citation intent not served by existing routes; reuses AnswerBlock + the on-demand pattern; activates the comparison word-count branch.
Output: comparison slug registry, FR+EN [slug] routes, ComparisonColumns component, authored copy for 4 pages × 3 tenants × 2 locales, comparison decision dict key.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-net-new-pages/04-CONTEXT.md
@.planning/phases/04-net-new-pages/04-RESEARCH.md
@.planning/phases/04-net-new-pages/04-UI-SPEC.md
@.planning/phases/04-net-new-pages/04-03-SUMMARY.md
</context>

## Artifacts this phase produces (04-04)

| Artifact | Kind | Notes |
|----------|------|-------|
| `src/lib/comparisons.ts` | NEW registry | 4 records, each slug:{fr,en}, mirrors services.ts |
| `src/app/[lang]/comparaisons/[slug]/page.tsx` | NEW FR route | [slug] resolver |
| `src/app/[lang]/comparisons/[slug]/page.tsx` | NEW EN route | [slug] resolver |
| `src/components/ComparisonColumns.tsx` | NEW component | two-column cards |
| `pages.comparison.{4 slugs}.*` per-tenant copy | authored JSON | ≥200-word body each |
| `dict.cta`/comparison decision heading keys | dict | "Laquelle choisir ?" / "Which should you choose?" |
| comparison branch of checkWordCount + answerBlock presence | guard activation | fully live |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: comparison registry + ComparisonColumns + authored copy + activate comparison word-count</name>
  <files>src/lib/comparisons.ts, src/components/ComparisonColumns.tsx, src/config/tenants/{ongles-maily,ongles-charlesbourg,ongles-rivieres}/seo.fr.json, .../seo.en.json, src/dictionaries/fr.json, src/dictionaries/en.json, src/config/schema-invariants.ts, src/config/schema-invariants.test.ts</files>
  <read_first>
    - src/lib/services.ts (serviceBySlug/servicePathsByLocale pattern to mirror for comparisons)
    - src/config/schema-invariants.ts (checkWordCount comparison branch + ANSWER_BLOCK_ROUTES + NEW_COMPARISON_SLUGS from 04-01; validateSchemaInvariants wiring)
    - src/config/tenants/ongles-maily/seo.fr.json (pages.comparison scaffold)
    - .planning/phases/04-net-new-pages/04-UI-SPEC.md §B (two-column cards template)
    - src/components/AnswerBlock.tsx
  </read_first>
  <behavior>
    - comparisonBySlug(lang, slug) resolves a FR or EN slug to its record; unknown → undefined
    - after authoring, checkWordCount returns zero errors for all 4 comparison bodies (≥200 words) in both locales; a fail-fixture proves it fires under 200
    - answerBlock presence covers all 4 comparison routes (≥2 sentences)
  </behavior>
  <action>
    Create src/lib/comparisons.ts exporting a COMPARISONS registry of 4 records, each `{ id, slug: { fr, en }, services: [...] }` for pose-vs-remplissage, manucure-vs-pedicure, gel-vs-acrylique, meilleur-pour (FR slugs match the pages.comparison keys; EN slugs idiomatic e.g. nail-extensions-vs-fill, manicure-vs-pedicure, gel-vs-acrylic, best-for — planner discretion, keep stable). Add comparisonBySlug(lang, slug), comparisonPath(record, lang), comparisonPathsByLocale(record) mirroring services.ts. Create ComparisonColumns.tsx (server component): `grid md:grid-cols-2 gap-8`, each column a white shadow-card with `<h2>` option name + 1-2 sentence descriptor + 3-5 attribute bullets; both columns reflect real services (gel AND acrylic both real, P-06); single column on mobile. Author pages.comparison.{4 slugs} in all 3 tenants' seo.fr.json + seo.en.json (replace 04-01 placeholders): answerHeading (verdict-first), answerBlock (≥2 sentences, first sentence stands alone D-22), body (≥200 words unique, qualitative pricing D-29, vous D-21, idiomatic EN D-24). For meilleur-pour compose durability + occasions into one body (P-07; occasions = planner discretion). Keys identical fr/en. Add a comparison decision heading dict key to dictionaries/fr.json ("Laquelle choisir ?") + en.json ("Which should you choose?"). Then ACTIVATE: ensure checkWordCount's comparison branch (≥200) is live for all 4 slugs and extend ANSWER_BLOCK presence to comparison routes (add the 4 comparison route identifiers to the answer-block coverage). Add fail-fixture test: comparison body of 10 words → checkWordCount error. Run tests + build.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts src/config/seo/seo-parity.test.ts && bun run build 2>&1 | tail -15</automated>
  </verify>
  <done>comparisons.ts registry + ComparisonColumns exist; 4 comparison bodies ≥200 words authored + parity-clean × 3 tenants × 2 locales; comparison word-count + answerBlock presence live + green; fail-fixture proves word-count bites; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FR + EN comparison [slug] route pages (shared template)</name>
  <files>src/app/[lang]/comparaisons/[slug]/page.tsx, src/app/[lang]/comparisons/[slug]/page.tsx</files>
  <read_first>
    - src/app/[lang]/services/[slug]/page.tsx ([slug] resolver pattern: serviceBySlug → notFound, generateMetadata routeByLocale)
    - src/app/[lang]/tarifs/page.tsx (04-02 sibling for JsonLd/AnswerBlock/CTA composition)
    - src/lib/comparisons.ts (from Task 1)
    - src/components/ComparisonColumns.tsx (from Task 1)
    - .planning/phases/04-net-new-pages/04-UI-SPEC.md §B
  </read_first>
  <behavior>
    - /fr/comparaisons/gel-vs-acrylique → 200; /en/comparisons/gel-vs-acrylic → 200; wrong-locale slug → 404
    - renders AnswerBlock → ComparisonColumns → which-to-choose → CTA
  </behavior>
  <action>
    Create comparaisons/[slug]/page.tsx (FR) and comparisons/[slug]/page.tsx (EN) following the service-detail [slug] template EXACTLY: no generateStaticParams / per-file dynamic; `if (!isLocale(lang)) notFound()` first; resolve `comparisonBySlug(lang, slug)` → notFound() if missing or wrong-locale (FR folder rejects en slugs and vice versa via the locale-specific slug lookup). generateMetadata: pageMetadata(lang, comparisonPath(record, lang), { title, description from pages.comparison[record.id] meta or derive, routeByLocale: comparisonPathsByLocale(record) }). Default export: getStoreConfig/getSeo/getDictionary; render breadcrumbGraph JsonLd (Home → Comparisons → record); `<AnswerBlock heading={seo.pages.comparison[record.id].answerHeading} text={seo.pages.comparison[record.id].answerBlock} />` first-in-main (single h1); `<ComparisonColumns .../>` from the record + copy; a "which to choose" `<h2>` (the comparison decision dict key) + decision prose block (max one gold highlight per UI-SPEC §B); CTA Button → booking + cross-links to the relevant service detail(s) + /tarifs (P-19). Both files share structure; EN differs by slug resolution + locale guard. Run build.
  </action>
  <verify>
    <automated>bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>FR + EN comparison [slug] routes compile; build green; all 4 slugs reachable per locale via the resolver; template order AnswerBlock→columns→decision→CTA; single h1 per page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — 4 comparison pages answer-first, cards, ≥200 words, locale-correct</name>
  <action>Human confirms the 4 comparison pages render as cards, lead with the verdict, meet word count, and respect locale guards before this slice closes. No code change — verification only.</action>
  <what-built>4 comparison/decision pages on a shared two-column-card template: answer-first verdict h1, ≥200-word unique body, real-service columns, which-to-choose decision block, CTA + cross-links.</what-built>
  <how-to-verify>
    1. `bun run dev` (TENANT=ongles-maily). Visit /fr/comparaisons/gel-vs-acrylique — confirm verdict in the first sentence (h1 block), two side-by-side cards (Gel / Acrylique) with attribute bullets, a "Laquelle choisir ?" decision block, and a Book CTA. Confirm it is cards, not a dense data table.
    2. Visit the other three FR slugs (pose-vs-remplissage, manucure-vs-pedicure, meilleur-pour) — each ≥200 words, answer-first.
    3. Visit the EN equivalents (e.g. /en/comparisons/gel-vs-acrylic) — idiomatic EN, same structure.
    4. Visit a wrong-locale URL (/en/comparaisons/gel-vs-acrylique) — confirm 404.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (table instead of cards, <200 words, verdict not first, locale leak).</resume-signal>
</task>

</tasks>

<verification>
- `bun test src/` green (comparison word-count fail-fixture + parity)
- `bun run build` green with comparison ≥200 branch live
- `curl` each of 4 FR slugs + 4 EN slugs → 200; one wrong-locale → 404
- seo-parity test green for pages.comparison.* keys
</verification>

<success_criteria>
PAGE-02 success criterion 2 met: ≥4 comparison/decision pages exist per tenant with answer blocks and ≥200 words unique copy, reachable FR+EN.
</success_criteria>

<output>
Create `.planning/phases/04-net-new-pages/04-04-SUMMARY.md` when done.
</output>
