---
phase: 04-net-new-pages
plan: 05
type: execute
wave: 5
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - src/app/sitemap.ts
  - src/components/Header.tsx
  - src/config/tenants/ongles-maily/site.ts
  - src/config/tenants/ongles-charlesbourg/site.ts
  - src/config/tenants/ongles-rivieres/site.ts
  - src/config/tenants/template/site.ts
  - src/config/schema-invariants.ts
  - src/config/schema-invariants.test.ts
  - src/dictionaries/fr.json
  - src/dictionaries/en.json
  - .planning/phases/04-net-new-pages/04-UAT.md
autonomous: false
requirements: [PAGE-01, PAGE-02, PAGE-03]
user_setup: []
must_haves:
  truths:
    - "sitemap.ts emits all new routes for both locales with correct localized-slug hreflang via a localizedPageEntries block (pricing + 4 comparisons + 3 borough near-me)"
    - "Header nav includes pricing + the comparisons hub/entries (real route hrefs); near-me is NOT in header"
    - "checkRoutePresence is LIVE in validateSchemaInvariants — build fails if a tenant is missing a new route registration"
    - "Full build guard suite is green on all 3 tenants: word-count (200/150), <30% overlap, route-presence, answerBlock ≥2 sentences"
    - "04-UAT.md documents the manual gates: Google Rich Results (pricing), route reachability FR/EN, sitemap hreflang, AI-citation spot-check carry-forward"
  prohibitions:
    - "localized FR/EN-distinct slugs MUST NOT be added to site.routes (same-slug-per-locale assumption breaks sitemap — use localizedPageEntries, RESEARCH Q6/Pitfall 2)"
    - "near-me MUST NOT be added to site.nav (P-04)"
    - "template tenant must be updated in lockstep to avoid clone-source divergence (A5)"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "localizedPageEntries for all new routes, both locales, hreflang"
    - path: "src/config/schema-invariants.ts"
      provides: "checkRoutePresence wired into validateSchemaInvariants"
    - path: ".planning/phases/04-net-new-pages/04-UAT.md"
      provides: "manual verification checklist + automated coverage map"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "src/lib/seo.ts"
      via: "localizedPageEntries built with altLanguages-style per-locale path pairs"
      pattern: "tarifs|pricing|comparaisons|comparisons"
    - from: "src/config/schema-invariants.ts"
      to: "src/config/tenants/*/site.ts"
      via: "checkRoutePresence asserts every new route registered per tenant"
      pattern: "checkRoutePresence"
---

<objective>
Integration + activation slice for Success Criterion 4: wire every new route into sitemap.ts (both locales, correct hreflang), add pricing + comparisons to the header nav, activate the route-presence guard, run the full build-guard suite green on all tenants, and author the phase UAT. This closes the phase end-to-end.

Purpose: makes all net-new routes discoverable + indexable, locks the final build gate, and records the manual verification contract.
Output: localizedPageEntries in sitemap, header nav entries, checkRoutePresence live, full green build × 3 tenants, 04-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-net-new-pages/04-CONTEXT.md
@.planning/phases/04-net-new-pages/04-RESEARCH.md
@.planning/phases/04-net-new-pages/04-VALIDATION.md
@.planning/phases/04-net-new-pages/04-02-SUMMARY.md
@.planning/phases/04-net-new-pages/04-03-SUMMARY.md
@.planning/phases/04-net-new-pages/04-04-SUMMARY.md
</context>

## Artifacts this phase produces (04-05)

| Artifact | Kind | Notes |
|----------|------|-------|
| `localizedPageEntries` in sitemap.ts | NEW sitemap block | per-locale path pairs + hreflang for all new routes |
| Header nav entries (pricing + comparisons) | Header.tsx + site.nav/dict | real route hrefs; near-me excluded |
| `checkRoutePresence` wired | guard activation | build fails on missing route registration |
| `04-UAT.md` | NEW doc | manual gates + automated coverage map |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sitemap localizedPageEntries + checkRoutePresence activation</name>
  <files>src/app/sitemap.ts, src/config/schema-invariants.ts, src/config/schema-invariants.test.ts, src/config/tenants/{ongles-maily,ongles-charlesbourg,ongles-rivieres,template}/site.ts</files>
  <read_first>
    - src/app/sitemap.ts (altLanguages helper, pageEntries same-route assumption, serviceEntries localized pattern lines 36-54)
    - src/lib/comparisons.ts (comparisonPathsByLocale — from 04-04)
    - src/config/schema-invariants.ts (checkRoutePresence from 04-01; validateSchemaInvariants wiring; TENANT_REGISTRY)
    - src/config/tenants/ongles-maily/site.ts (routes array + nav array shape)
    - .planning/phases/04-net-new-pages/04-RESEARCH.md Q6 + Pitfall 2 (localizedRoutes vs in-sitemap handling)
  </read_first>
  <behavior>
    - sitemap output contains /fr/tarifs + /en/pricing (each with hreflang fr→/fr/tarifs, en→/en/pricing, x-default), all 4 comparison FR+EN slugs, and the 3 borough routes — no /en/tarifs or /fr/pricing leak
    - checkRoutePresence fires (build fails) if a tenant's site config is missing a registered new route; passes when all present
  </behavior>
  <action>
    In sitemap.ts add a `localizedPageEntries` block built from explicit `{ fr, en }` path pairs for the new localized routes (pricing: {fr:"/tarifs",en:"/pricing"}; the 4 comparisons via comparisonPathsByLocale; the 3 borough near-me routes — same slug both locales unless a record defines distinct ones). For each pair emit one sitemap entry per locale using the locale's own path, with `alternates.languages` = { fr: fr-path, en: en-path, "x-default": fr-path } (mirror the serviceEntries pattern, NOT the same-route pageEntries pattern — Pitfall 2). Do NOT add the distinct-slug routes to site.routes. Append localizedPageEntries to the returned array. Then finalize checkRoutePresence in schema-invariants.ts to assert each tenant registers all new routes (read the canonical new-route list from a shared constant; near-me belongs to site.routes per 04-03, pricing/comparisons are tracked via the localizedPageEntries source-of-truth constant) and WIRE it: `errors.push(...checkRoutePresence());` in validateSchemaInvariants. Confirm checkWordCount (comparison + near-me) and checkCrossTenantOverlap are all wired and live. Update template/site.ts in lockstep (A5). Add a fail-fixture test proving checkRoutePresence fires on a missing registration. Run tests + build × the 3 tenants.
  </action>
  <verify>
    <automated>bun test src/config/schema-invariants.test.ts && bun run build 2>&1 | tail -15</automated>
  </verify>
  <done>localizedPageEntries emits all new routes both locales with correct hreflang and no wrong-locale leak; checkRoutePresence live + green; full guard suite green; template updated; fail-fixture proves route-presence bites.</done>
</task>

<task type="auto">
  <name>Task 2: Header nav entries for pricing + comparisons</name>
  <files>src/components/Header.tsx, src/config/tenants/{ongles-maily,ongles-charlesbourg,ongles-rivieres,template}/site.ts, src/dictionaries/fr.json, src/dictionaries/en.json</files>
  <read_first>
    - src/components/Header.tsx (iterates site.nav lines 63-72, 101-116; dict.nav[item.key] label lookup)
    - src/config/tenants/ongles-maily/site.ts (nav array shape — anchor hrefs today)
    - src/dictionaries/en.json + fr.json (nav block; dict.nav.pricing added in 04-02)
    - .planning/phases/04-net-new-pages/04-UI-SPEC.md §Navigation & Cross-Linking
  </read_first>
  <behavior>
    - Header renders Pricing + Comparisons entries as real route links (not # anchors); clicking navigates to the route
    - near-me is absent from the header
  </behavior>
  <action>
    Add nav entries to each tenant's site.nav (and template) for pricing and comparisons as REAL route hrefs (locale-prefixed by Header as today). Pricing href is the locale's pricing slug; comparisons can be a single hub entry or the lead comparison — planner discretion, but render as real routes, a behavior change from anchor scroll (P-04, documented). Ensure dict.nav keys exist in BOTH locales: dict.nav.pricing (from 04-02), dict.nav.comparisons (add "Comparatifs" / "Comparisons") — F-02 dictionary parity must stay green (Pitfall 6). If Header currently assumes all nav hrefs are "#…" anchors, adjust the link handling so real route hrefs are rendered correctly for both desktop and mobile nav. Do NOT add near-me to nav. Run dictionary parity + build.
  </action>
  <verify>
    <automated>bun test src/config/seo/seo-parity.test.ts && bun run build 2>&1 | tail -15</automated>
  </verify>
  <done>Pricing + comparisons appear in header nav as real routes both desktop+mobile; dict.nav keys parity-clean; near-me excluded; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Author 04-UAT.md (manual gates + automated coverage map)</name>
  <files>.planning/phases/04-net-new-pages/04-UAT.md</files>
  <read_first>
    - .planning/phases/04-net-new-pages/04-VALIDATION.md (Manual-Only Verifications + Per-Task map)
    - .planning/phases/03-content-depth/03-UAT.md (format precedent)
    - .planning/ROADMAP.md Phase 4 success criteria (4 criteria)
  </read_first>
  <action>
    Write 04-UAT.md mapping each of the 4 ROADMAP success criteria to its verification: SC1 (pricing ItemList+AggregateOffer) → automated build guard + ItemList shape test + manual Google Rich Results Test on /fr/tarifs; SC2 (comparison ≥200 + AnswerBlock) → checkWordCount + answerBlock presence (automated) + visual DOM check; SC3 (near-me ≥150 + <30% overlap) → checkWordCount + checkCrossTenantOverlap (automated) + cross-tenant copy review; SC4 (all routes in sitemap, no 404 FR/EN) → sitemap XML grep + curl 200/404 smoke. Include the carry-forward AI-citation spot-check (ChatGPT/Perplexity "[salon] tarifs/pricing [city]" after indexing, non-blocking, like Phase 3 SC-4). Mark which gates are automated-green-now vs manual-post-deploy.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-net-new-pages/04-UAT.md && grep -c "Success Criterion\|SC" .planning/phases/04-net-new-pages/04-UAT.md</automated>
  </verify>
  <done>04-UAT.md exists, maps all 4 success criteria to automated/manual gates, includes the AI-citation carry-forward.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify — sitemap complete, nav correct, full guard suite green cross-tenant</name>
  <action>Human confirms sitemap coverage/hreflang, header nav membership, route reachability, and a green build across all tenants before the phase closes. No code change — verification only.</action>
  <what-built>All net-new routes wired into sitemap (both locales, correct hreflang) + pricing/comparisons in header nav + full build-guard suite green on all 3 tenants + phase UAT authored.</what-built>
  <how-to-verify>
    1. `bun run build` then `bun run start` (or dev) with TENANT=ongles-maily. Visit http://localhost:3000/sitemap.xml — confirm /fr/tarifs, /en/pricing, the 4 comparison FR+EN slugs, and /fr/beauport appear; confirm NO /en/tarifs or /fr/pricing entries; confirm each has hreflang alternates.
    2. Load the homepage — confirm Pricing + Comparisons now appear in the header nav and navigate to real routes (not anchor scroll). Confirm near-me is NOT in the header.
    3. Smoke each new route FR + EN for 200 and one wrong-locale for 404.
    4. Confirm `bun run build` is green for congles-charlesbourg and ongles-rivieres too (all guards pass cross-tenant).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (sitemap leak, nav missing/near-me leaked, 404 on a valid route, guard failure on a secondary tenant).</resume-signal>
</task>

</tasks>

<verification>
- `bun test src/` green; `bun run build` green on all 3 tenants
- `curl -s localhost:3000/sitemap.xml | grep -c "tarifs\|pricing\|comparaisons\|comparisons\|beauport"` → all new routes present; no /en/tarifs or /fr/pricing
- Header renders pricing + comparisons real routes; site.nav has no near-me entry
- checkRoutePresence + checkWordCount + checkCrossTenantOverlap + answerBlock presence all wired in validateSchemaInvariants
- 04-UAT.md present
</verification>

<success_criteria>
PAGE-01/02/03 success criterion 4 met: all new routes in sitemap and reachable without 404 on FR+EN; full deploy-blocking guard suite green; UAT documented.
</success_criteria>

<output>
Create `.planning/phases/04-net-new-pages/04-05-SUMMARY.md` when done.
</output>
