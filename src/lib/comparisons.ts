// Comparison decision-pages are not used by Pure Nail Bar. This module is kept
// as a neutral stub (the route was removed) so no stale references to the old
// service taxonomy remain. Safe to delete entirely once tooling permits.
export {};
